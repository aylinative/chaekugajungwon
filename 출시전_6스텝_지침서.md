# 책육아정원 — 출시 전 6스텝 실행 지침서 (작성 2026.08.26)

> 순차로 한 스텝씩 진행한다. Claude Code는 매 스텝 시작 시 이 문서의 해당 STEP을 먼저 읽고 그대로 따른다.
> CLAUDE.md 규칙(확신도 표기·완료 후 대기·JSX/import 사전검토)과 함께 적용.

## 공통 작업 방식 (모든 스텝 준수)
1. **한 번에 한 스텝, 순서대로.** 다음 스텝으로 자동 진행하지 않는다.
2. **'검토' 스텝(STEP 2·3·4)**: 먼저 옵션·결정사항을 정리해 **사용자 확정을 받은 뒤** 구현. 코드부터 짜지 않는다.
3. 각 스텝 흐름: **계획 보고 → 사용자 확정 → 구현 → `npm run build` 검증 → 커밋 → CLAUDE.md Deferred TODO 갱신(✅)**.
4. **DB 변경**: `supabase/*.sql` 마이그레이션 파일로 남기고 scratchpad `db.js`로 적용·검증. CHECK/RLS 영향 확인.
5. **배포 영향**: push 시 Vercel 자동 배포. 빌드·DB 일관성(예: 컬럼명) 확인 후 push.
6. **커밋 위생**: `git add`는 파일 명시(사용자 미커밋 작업 쓸어담지 않기). 사용자 문서(스파이크·작업지시서 등) 커밋 금지.
7. 각 스텝 완료 시 이 지침서에도 진행상태(☐→✅)를 표시.

---

## ✅ STEP 1. 홈 보드북 표현 (완료 2026.08.26, 커밋 630e5e2)
**목표:** `books.is_board_book`(이미 저장됨)를 홈 피드에서 노출. 보드북 여부는 **씨앗·새싹(0~18M)** 시기에 특히 유용한 물성 정보.
**결과:** feed.ts에 is_board_book 배선 + BookCard `showBoardBook` prop → 씨앗·새싹 섹션 카드 표지 좌상단 '보드북' 칩. 홈·그룹 전체보기 적용.

**현황:**
- DB `books.is_board_book`(bool) 존재. 기록 폼 체크박스로 입력됨.
- 홈 카드(`BookCard`)·`feed.ts` 집계에는 `is_board_book`이 아직 없음 → 데이터부터 실어야 함.

**결정할 것(구현 전 확정):**
- (a) **카드에 작은 '보드북' 배지** — 어느 섹션? 씨앗·새싹만 vs 전 섹션. (권장: 씨앗·새싹 섹션 카드에만 — 정보 과밀 방지 + 유용성 집중)
- (b) **'보드북만' 토글/필터** — 씨앗·새싹 섹션 헤더에 추가 (후속 가능)
- 권장안: **(a) 배지부터**(저비용·정보성), (b) 토글은 후속.

**구현 범위·파일:**
- `src/lib/feed.ts`: `aggregateBookCards` posts 조회의 `book:books(...)`에 `is_board_book` 추가 → `BookCard` 인터페이스에 `isBoardBook: boolean` → 카드 매핑.
- `src/components/feed/BookCard.tsx`: 씨앗·새싹 섹션에서만(또는 결정대로) 표지/제목 옆 작은 '보드북' 배지. 섹션 판별 위해 카드에 group 정보 또는 섹션에서 prop 전달.
- (선택 b) `GroupSection.tsx`/`page.tsx`: 씨앗·새싹 섹션에 '보드북만' 필터.

**검증:** 보드북=true 책이 씨앗·새싹 카드에 배지로 보이는지. 빌드 통과.

---

## ✅ STEP 2. 홈 피드 '우리 아이 기준' 정렬 (완료 2026.08.27, 커밋 0bd916b)
**확정·구현:** 옵션 B(토글). 상단 [일반 순서/우리 아이 기준], 기본=일반(연령 오름차순 원칙 유지),
로그인+아이 있을 때만 노출. '우리 아이 기준'=아이 시기 먼저(첫째 순, 중복 제거)→나머지 오름차순.
클라이언트 재정렬(FeedSections에 서버 렌더 GroupSection 노드 전달, 새로고침 없음).
**목표(원문):** 내 아이 현재 개월에 해당하는 시기 그룹을 홈에서 먼저 보이게 하거나, '일반 순서 ↔ 우리 아이 기준' 선택 필터 제공.

**현황·제약:**
- 홈은 6개 시기 섹션을 **연령 오름차순 고정**으로 노출(CLAUDE.md 7장: 시기는 순서형 변수, x축/섹션 순서가 곧 정보). → **섹션 순서를 무조건 바꾸면 이 원칙과 충돌.**
- 아이 현재 개월→시기: `lib/groups.ts getGroupValueByMonths` 존재. 아이 생년월일은 `children`(로그인 유저).

**결정할 것(구현 전 확정) — 옵션:**
- (A) **섹션 순서 재정렬**: 아이 시기 섹션을 맨 위로. → 원칙 충돌, 비로그인/무자녀 처리 필요. 비권장(기본값으로는).
- (B) **정렬 토글**: 상단에 `[일반 순서] [우리 아이 기준]`. 기본=일반(원칙 유지), 켜면 아이 시기 먼저. **사용자 자발적 선택이라 원칙과 공존.** (권장)
- (C) **'우리 아이 시기' 바로가기/핀**: 섹션 순서는 유지하되, 상단에 "우리 아이(○○개월) 추천 보기" 칩 → 해당 섹션으로 스크롤 또는 그 그룹 전체보기(/group/[value])로. (가장 가벼움, 원칙 100% 유지)
- 권장: **(C) 먼저**(저비용·원칙유지) 또는 **(B) 토글**. (A)는 지양.
- 부수 결정: 아이 2명 이상이면 어느 아이 기준?(선택 아이/최근/전부), 비로그인·무자녀는 일반 순서 폴백.

**구현 범위·파일:**
- 아이 현재 개월 → 시기 value 계산(서버, `page.tsx`에서 유저 children 조회 + `getGroupValueByMonths`).
- (B) 클라이언트 토글 컴포넌트 + 섹션 재정렬 로직 / (C) 상단 바로가기 칩(기존 /group 라우트 재사용).
- `src/app/page.tsx`, `src/lib/feed.ts`(필요시).

**검증:** 로그인+아이 있는 유저에게 우리 아이 시기가 먼저/강조되는지. 비로그인·무자녀 폴백. 빌드 통과.

---

## ☐ STEP 3. 책육아 통계 박스 + 완독 캘린더 (검토)
**목표:** 마이페이지에 '책육아 통계' 박스(지금까지·이번달·이번주 읽은 책 3섹션) + '완독 캘린더'(일자별 표지, 대표+N). 리다(독서기록앱) 완독 캘린더 참고.

**현황:**
- 마이페이지는 '우리집 책장 · N권'만 표기(정식 통계 박스는 미구현).
- 데이터: `posts.created_at`(기록 작성일). ⚠️ **작성일 ≠ 실제 읽은 날**(CLAUDE.md 10.1 경고). → 캘린더/통계는 "기록한 날" 기준임을 UI에 반영하거나 명칭을 '기록'으로.

**결정할 것(구현 전 확정):**
- 통계 '읽은 책' 단위: 책(중복 제거) vs 기록 수. 기간: 이번달/이번주 경계(로컬 타임존).
- 캘린더: 월 그리드 + 좌우 이동. 각 날짜 셀에 그 날 기록한 책 대표 표지 1 + '+N'. 탭 시 그 날 목록/책 페이지.
- 명칭: '완독'인지 '기록'인지(작성일 기준이므로 '기록 캘린더'가 정확할 수 있음) — 사용자 확정.
- **리다 완독 캘린더를 브라우저(claude-in-chrome)로 플레이스토어에서 직접 보고 UI 참고** 후 초안.

**구현 범위·파일:**
- `mypage/page.tsx`: posts에 created_at 조회(이미 있음) → 기간 집계.
- 새 컴포넌트: `StatsBox`(3섹션), `ReadingCalendar`(client, 월 이동 상태). 표지는 `BookCover` 재사용.

**검증:** 이번달/이번주 카운트 정확, 캘린더 날짜별 표지·+N 정확, 타임존 경계. 빌드 통과. (규모 큼 — 통계 박스 먼저, 캘린더 별도 커밋 가능)

---

## ☐ STEP 4. 첫 접속 사용법 모달 + 사용법 페이지 (검토)
**목표:** 첫 접속 시 사용법 모달/배너 팝업 + 우상단(또는 마이페이지)에서 언제든 볼 수 있는 사용법 페이지.

**현황:** 없음. 단, 첫 기록 게이트(11.4: 가입→온보딩→첫 기록→홈)가 이미 있어 신규 유저 흐름과 조율 필요.

**결정할 것(구현 전 확정):**
- '첫 접속' 판별: **localStorage 플래그**(기기별, 간단·서버무관, 권장) vs users DB 플래그(기기 바뀌어도 유지).
- 형태: 모달(1회, 닫기) vs 상단 배너. 첫 기록 게이트와 겹치지 않게(게이트 통과 후 홈 첫 진입 시 1회 권장).
- 내용: 핵심 사용법 3~5장(기록하기·시기·우리 아이 반응·추천/저장·검색). 카피 확정 필요.
- 사용법 페이지 위치: `/guide`(또는 `/help`) + 진입점(상단 헤더 아이콘 or 마이페이지 링크).

**구현 범위·파일:**
- `UsageGuideModal`(client, localStorage 1회), `/app/guide/page.tsx`(정적), 헤더/마이 진입 링크.

**검증:** 첫 접속 1회만 노출, 다시 안 뜸, 사용법 페이지 접근. 빌드 통과.

---

## ☐ STEP 5. 배포 전 정리
**목표:** 공개 전 샘플·테스트 상태·검증 파일 정리. (⚠️ 실제 공개 직전에 — QA 다 끝난 뒤 실행)

**체크리스트:**
- [ ] `supabase/cleanup_sample.sql` 실행 (샘플: `aaaaaaaa-` 유저 10, `bbbbbbbb-`/`[샘플]` 책·기록). db.js로 적용·검증.
- [ ] 테스트 상태 원복: `is_operator=true`(서점 다니는 워킹맘) 판단, 테스트로 숨긴 기록(cccccccc-...) 복구 판단.
- [ ] 스파이크 파일 삭제: `spike_book_search.mjs`, `spike_rank_test.mjs`.
- [ ] 잔여 dev 페이지 없는지 확인(`/dev/*`).
- [ ] (선택) `schema.sql` 드리프트 정리(재해복구용 진실성 — 라이브 무해).
- [ ] 최종 `npm run build` 통과 확인.

**검증:** 샘플 데이터 0건, 피드/책장이 실데이터만.

---

## ☐ STEP 6. 도메인 구매 후 반영
**목표:** 실도메인 구매(후보 bookgardening.com) → Vercel·OG·로그인 리다이렉트에 반영.

**절차:**
1. 도메인 구매(사용자).
2. Vercel 프로젝트 → Domains에 커스텀 도메인 추가 + DNS 설정(안내).
3. Vercel 환경변수 `NEXT_PUBLIC_SITE_URL` = `https://<실도메인>` → **재배포**(OG·sitemap 절대 URL 반영).
4. **로그인 되게**: Supabase Auth Redirect URLs + 카카오 개발자 콘솔 Redirect에 실도메인 추가(코드는 origin 동적화라 자동 대응).
5. 확인: 실도메인 접속·카카오 로그인·카톡 OG 미리보기.

**검증:** 실도메인에서 로그인·공유·sitemap(`/sitemap.xml`)이 실도메인으로.

---

## ✅ STEP 6-1. 홈 화면 아이콘·앱 이름 (PWA/파비콘) — 완료(2026.08.28)
**목표:** 모바일에서 브라우저 '홈 화면에 추가' 시 뜨는 **아이콘·이름을 Vercel 기본 → 책육아정원 커스텀**으로.

**현황:** 커스텀 아이콘/매니페스트가 없어 브라우저가 기본(Vercel/파비콘)으로 폴백. (이 서비스는 모바일 웹이라 홈 바로가기 아이콘이 첫인상)

**구현 (Next.js App Router 파일 관례 — src/app/ 아래):**
- `app/icon.png` — 브라우저 탭 파비콘 (자동 인식)
- `app/apple-icon.png` — iOS Safari '홈 화면에 추가' 아이콘 (180×180 권장)
- `app/manifest.ts`(or manifest.json) — Android Chrome PWA:
  - `name: '책육아정원'`, `short_name: '책육아정원'`, `description`
  - `icons: [{192×192}, {512×512}]`(purpose any/maskable), `theme_color`·`background_color`(한지 아이보리/모스그린), `display: 'standalone'`, `start_url: '/'`
  - layout.tsx metadata에 manifest 연결(App Router는 app/manifest.ts 자동 인식)

**필요 자산:** 로고/아이콘 이미지(정사각). 없으면 임시로 🌿+아이보리 배경의 심플 아이콘 생성(사용자 확정).

**결정할 것:** 아이콘 디자인(로고 제공 vs 임시 생성), 앱 이름 표기('책육아정원').

**검증:** 실기기(iOS Safari / Android Chrome)에서 '홈 화면에 추가' → 커스텀 아이콘·이름 확인.
**순서:** 공개(STEP 5·6) 즈음. 도메인과 독립이라 먼저 해도 무방하나, 아이콘 자산 준비 필요.

---

## 진행 로그
- 2026.08.26 지침서 작성. STEP 1(홈 보드북 표현) ✅ 완료. STEP 6-1(홈 화면 아이콘) 추가.
- 2026.08.28 STEP 6-1 ✅ 완료. home_image.png(나무+아기+책 로고)로 아이콘 생성:
  app/icon.png(512)·app/apple-icon.png(180)·public/icon-192·512.png + app/manifest.ts(name '책육아정원',
  standalone, 배경 #FBF6EC·테마 #6B7F5E 임시토큰). manifest/아이콘 링크 자동주입·서빙 검증. 실기기 확인만 남음.
