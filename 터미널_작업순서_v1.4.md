# 책육아정원 — 6.5 / 6.7단계 작업 순서 (2026.07.31)

프로젝트 경로: `C:\Users\김가현\chaekugajungwon`
기준: CLAUDE.md v1.3.1 (65%, 커밋 049e8e3) → v1.4

**표기**
- 🧑 = 사람이 직접 (Supabase 콘솔·브라우저·백업)
- 🤖 = Claude Code 터미널에 붙여넣을 요청문
- SQL 파일은 **가 · 나 · 다** 로 부른다 (STEP 번호와 헷갈리지 않도록)

**⚠️ 한글 파일 조작**
PowerShell `>>` `>` 리다이렉션은 한글 인코딩이 깨진다.
파일 생성·수정은 반드시 Claude Code 또는 에디터에서 직접 할 것.

---

# 전체 순서

| STEP | 내용 | 되돌리기 |
|---|---|---|
| 0 | 커밋 + CLAUDE.md 교체 + diff 확인 | 쉬움 |
| 1 | DB 마이그레이션 (가·나·다) | **어려움** |
| 2 | 알라딘 외국도서 (E) | 쉬움 |
| 3 | 저장(북마크) (B) | 보통 |
| 4 | 기록 폼 개편 (C·F) + 아이 나이 표시 제거 | 쉬움 |
| 5 | 나도 추천해요 + 바텀시트 (D) | 보통 |
| 6 | 책 단위 묶기 + /book/[isbn] + 정렬 (G) | 보통 |
| 7 | 문서 갱신 + 커밋 | 쉬움 |

STEP 0~1을 하루, 2~4를 하루, 5~6을 하루로 나누는 걸 권한다.
STEP 1은 되돌리기가 어려우니 다른 작업과 섞지 말 것.

**왜 정렬 로직이 STEP 6에 있나**
17장에 "홈 피드를 책 1권 = 카드 1개로 묶을 예정"이 적혀 있다. 카드 단위가 바뀌면 집계 기준도 바뀐다. 정렬을 먼저 만들면 두 번 짜게 되므로 책 단위 묶기와 같은 STEP에서 처리한다.

---

# STEP 0 — CLAUDE.md 교체

## 0.1 🧑 커밋 (비교 기준점 만들기)

```powershell
cd C:\Users\김가현\chaekugajungwon
git add -A
git commit -m "chore: v1.4 작업 전 체크포인트"
```

## 0.2 🧑 CLAUDE.md 교체

에디터(VS Code / Cursor)에서 `CLAUDE.md` 를 연다.

1. `Ctrl+A` → `Delete` (전체 삭제)
2. 받은 `CLAUDE.md` 완성본 내용을 전부 붙여넣기
3. `Ctrl+S` 저장

## 0.3 🧑 무엇이 바뀌었는지 확인

```powershell
git diff CLAUDE.md
```

- **빨간 줄(`-`)** = 지워진 내용
- **초록 줄(`+`)** = 새로 들어온 내용

한 화면씩 넘기려면 스페이스바, 빠져나오려면 `q`.

바뀐 줄 수만 세려면:

```powershell
git diff --stat CLAUDE.md
```

## 0.4 🧑 체크

- [ ] `git diff` 결과가 v1.4 변경사항과 일치 (전체가 바뀐 것처럼 나오면 형식이 어긋난 것)
- [ ] 한글 깨짐 없음
- [ ] 10.3 절과 9.2 절이 새로 생겼음
- [ ] 20장 맨 아래에 v1.4 행이 있음

**⚠️ Claude Code에게 CLAUDE.md를 수정하라고 시키지 말 것.** 이미 완성본으로 교체했으므로 추가 작업이 필요 없다.

---

# STEP 1 — DB 마이그레이션

## 1.0 🧑 현재 스키마 확인 (조회만, 안전)

Supabase → SQL Editor:

```sql
select table_name, column_name, data_type, is_nullable
  from information_schema.columns
 where table_name in ('books','bookmarks','likes')
 order by table_name, ordinal_position;
```

```sql
select conrelid::regclass as tbl, conname, contype, pg_get_constraintdef(oid)
  from pg_constraint
 where conrelid in ('bookmarks'::regclass, 'likes'::regclass);
```

```sql
select 'bookmarks' t, count(*) from bookmarks
union all select 'likes', count(*) from likes;
```

**확인할 것**
- `books.id` 타입이 uuid인지 → 아니면 아래 '나'의 `book_id uuid` 를 실제 타입으로 교체
- `bookmarks` 의 PK 구성
- `bookmarks` 행 수 → **0에 가까우면 '나'의 위험도가 사실상 없다**

## 1.1 🤖 SQL 파일 3개 생성

```
supabase/ 폴더에 아래 3개 파일을 만들어줘. 내용은 그대로, 변형하지 말 것.
UTF-8로 저장하고, 생성 후 각 파일의 첫 3줄을 보여주고 대기할 것.

── supabase/add_like_groups.sql ──
alter table likes
  add column if not exists group_names text[] not null default '{}';

alter table likes
  add constraint chk_likes_groups
  check (group_names <@ ARRAY['씨앗','새싹','봄꽃','사과','나무','어른']::text[]);

create index if not exists idx_likes_group_names on likes using gin (group_names);

── supabase/migrate_bookmarks_to_book.sql ──
alter table bookmarks
  add column if not exists book_id uuid references books(id) on delete cascade;

update bookmarks b
   set book_id = p.book_id
  from posts p
 where b.post_id = p.id
   and b.book_id is null;

delete from bookmarks a
 using bookmarks b
 where a.user_id = b.user_id
   and a.book_id = b.book_id
   and a.ctid    > b.ctid;

delete from bookmarks where book_id is null;

alter table bookmarks drop column if exists post_id;
alter table bookmarks alter column book_id set not null;
alter table bookmarks
  add constraint bookmarks_user_book_unique unique (user_id, book_id);

── supabase/add_bookmark_counter.sql ──
alter table books
  add column if not exists bookmark_count integer not null default 0;

create or replace function sync_book_bookmark_count()
returns trigger
language plpgsql
security definer
set search_path = public
as $$
begin
  if (TG_OP = 'INSERT') then
    update books set bookmark_count = bookmark_count + 1
     where id = NEW.book_id;
  elsif (TG_OP = 'DELETE') then
    update books set bookmark_count = greatest(bookmark_count - 1, 0)
     where id = OLD.book_id;
  end if;
  return null;
end;
$$;

drop trigger if exists trg_bookmarks_count on bookmarks;
create trigger trg_bookmarks_count
after insert or delete on bookmarks
for each row execute function sync_book_bookmark_count();

update books b
   set bookmark_count = coalesce(c.cnt, 0)
  from (select book_id, count(*) as cnt from bookmarks group by book_id) c
 where b.id = c.book_id;
```

## 1.2 🧑 Supabase SQL Editor에서 가 → 나 → 다 순서로 실행

| | 파일 | 하는 일 | 위험도 |
|---|---|---|---|
| **가** | `add_like_groups.sql` | `likes`에 컬럼 추가 | 안전 (추가만) |
| **나** | `migrate_bookmarks_to_book.sql` | `bookmarks` 구조 변경 | **위험 — 행·컬럼 삭제 포함** |
| **다** | `add_bookmark_counter.sql` | `books`에 컬럼 + 트리거 | 안전 (추가만) |

**순서 필수.** '다'를 먼저 실행하면 `book_id`가 없어 실패한다.

'나'를 실행한 뒤, 1.0에서 PK가 `(user_id, post_id)`였다면 추가 실행:

```sql
alter table bookmarks add primary key (user_id, book_id);
```

## 1.3 🧑 검증

```sql
select column_name, data_type from information_schema.columns
 where table_name = 'likes' and column_name = 'group_names';

select column_name from information_schema.columns
 where table_name = 'bookmarks';

select column_name, column_default from information_schema.columns
 where table_name = 'books' and column_name = 'bookmark_count';

select tgname from pg_trigger where tgrelid = 'bookmarks'::regclass;
```

`bookmarks` 결과에 `post_id`가 없고 `book_id`가 있으면 성공.

## 1.4 🤖 RLS 정책 수정

```
supabase/rls_policies.sql 을 열어서, bookmarks 관련 정책이 post_id를 참조하면
book_id로 수정해줘.

- 정책의 의미는 유지: 북마크는 본인만 읽기 / 본인만 쓰기.
- books 테이블은 누구나 읽기이므로 bookmark_count 조회에는 별도 정책이 필요 없다.
- 수정된 부분만 diff로 보여주고 대기할 것. 직접 실행하지는 말 것.
```

## 1.5 🧑 수정된 `rls_policies.sql` 을 SQL Editor에서 재실행

---

# STEP 2 — 알라딘 외국도서 (예정 기능 E)

가장 독립적이라 먼저 처리한다. 실패해도 다른 기능에 영향 없음.

**⚠️ 지금 버그가 있다.** `CATEGORY_IDS`에 들어 있는 `106165`는 **외국도서 트리의 CID**다. `SearchTarget=Book`으로는 도달할 수 없어 **네 번의 병렬 호출 중 하나가 빈 응답을 받고 있다.** 이 STEP에서 제자리로 옮긴다.

## 2.1 🤖

```
src/app/api/aladin/search/route.ts 를 수정해줘.
CLAUDE.md 13.1 의 [A][B][C] 규격을 그대로 따를 것.

1. CATEGORY_IDS 에서 106165 를 제거한다 → [1108, 13789, 2030]
   (106165는 국내도서가 아니라 외국도서 CID라 SearchTarget=Book으로는 결과가 없다)
2. FOREIGN_CATEGORY_IDS = [106165] 상수를 만들고,
   SearchTarget=Foreign 으로 별도 병렬 호출한다. MaxResults=10.
3. 국내/외국 결과를 Promise.all로 모아 isbn13(없으면 link) 기준 중복 제거.
4. 정렬은 국내도서 우선, 그 뒤 외국도서.
5. mallType 필터(BOOK·FOREIGN만 통과) 기존 로직 유지.
6. 한쪽 호출이 실패해도 다른 쪽 결과는 반환할 것.

수정 전에 현재 파일 구조를 보여주고 계획을 말한 뒤 대기할 것.
```

## 2.2 🧑 확인

- [ ] `달님 안녕` 검색 → 나옴 (번역 그림책은 국내도서)
- [ ] `배고픈 애벌레` 검색 → 나옴
- [ ] `Brown Bear` 검색 → 영어 원서 나옴
- [ ] 같은 책이 두 번 뜨지 않음

---

# STEP 3 — 저장(북마크) (예정 기능 B)

## 3.1 🤖

```
저장(북마크) 기능을 구현해줘. CLAUDE.md 12장 '저장(북마크)' 항목과 18.3-B 기준.

1. 저장 토글 API — POST/DELETE. 단위는 book_id (post_id 아님).
   UNIQUE(user_id, book_id) 제약이 있으므로 중복 저장 시 에러 대신 무시 처리.
2. 게시물 상세(src/app/posts/[id]/page.tsx)의 ③반응 영역에 저장 버튼 추가.
3. 홈 피드 카드(src/components/feed/*)에 저장 버튼 추가.
4. 저장 수 표시:
   - books.bookmark_count 를 읽어서 표시 (직접 count 쿼리 하지 말 것)
   - src/lib/constants.ts 에 BOOKMARK_COUNT_MIN_DISPLAY = 5 상수를 만들고,
     이 값 미만이면 카운트 영역을 아예 렌더링하지 않을 것 (0도 표시 금지)
   - 누가 담았는지 명단을 조회하는 API나 화면은 만들지 말 것
5. 마이페이지(src/app/mypage)를 두 목록으로 분리 (11.3):
   ① 아이와 지금까지 읽은 책 (posts) — 기존
   ② 나중에 아이와 읽으려고 저장한 책 (bookmarks) — 신규

주의:
- bookmark_count는 DB 트리거가 자동 갱신한다. 코드에서 직접 증감시키지 말 것.
- 모바일 우선 반응형(제1원칙).
- JSX 태그 쌍과 import 누락을 사전 검토할 것.

구현 전에 수정/생성할 파일 목록을 보여주고 대기할 것.
```

## 3.2 🧑 확인

- [ ] 저장 → 마이페이지 ②목록에 뜸
- [ ] 같은 책의 다른 기록을 저장해도 목록에 **1권만** 뜸
- [ ] Supabase `books.bookmark_count` 자동 증가
- [ ] 해제 시 감소
- [ ] 5명 미만일 때 카운트가 화면에 **안 보임**

---

# STEP 4 — 기록 폼 개편 (예정 기능 C·F)

## 4.1 🤖 세그먼트 버튼 + 반응 ⓘ

```
기록 폼(src/app/recommend/create/page.tsx)을 개편해줘.
CLAUDE.md 9.2 '시기 선택 UI'의 구조와 설계 근거를 그대로 따를 것.

1. 시기 칩 위에 세그먼트 버튼 2개 추가.
   [지금 읽고 있어요] [예전에 읽었어요] — 단일 선택, 기본값 '지금 읽고 있어요'
2. '지금 읽고 있어요' 상태:
   - 프로필 아이의 현재 월령에 해당하는 시기 칩 1개를 자동 체크 (src/lib/age.ts 활용)
   - 자동 체크는 반드시 1개만
3. '예전에 읽었어요' 탭 시:
   - 시기 칩 체크를 전부 해제
   - 칩 아래에 한 줄 힌트: "그때 시기를 골라주세요"
   - 이 힌트는 '예전에 읽었어요'일 때만 렌더링
4. 칩은 세그먼트와 무관하게 언제든 다중 선택·해제 가능
5. 아이가 2명 이상이면 아이 탭 선택 → 해당 아이 기준 자동 체크
6. 아이 정보가 없으면 세그먼트를 숨기고 빈 칩만 노출
7. 검증: '예전에 읽었어요' + 칩 미선택으로는 제출 불가. 인라인 에러.
8. '우리 아이 반응' 3단계 옆 ⓘ 아이콘 추가.
   문구: "솔직한 반응이 다른 양육자들에게 도움이 돼요"

절대 하지 말 것:
- 설명 캡션으로 자동 선택을 안내하지 말 것 (컨트롤로 표현하는 것이 이 설계의 핵심)
- 모달·알림창·바텀시트를 쓰지 말 것
- ⓘ 문구에 "홈 피드에 노출됩니다" 같은 노출 조건을 넣지 말 것 (반응 인플레이션 방지)

구현 전에 현재 시기 선택 영역 코드를 보여주고 계획을 말한 뒤 대기할 것.
```

## 4.2 🤖 아이 나이 표시 제거

```
CLAUDE.md 10.1 기준으로 게시물 상세 페이지를 수정해줘.

1. ① 책 정보 영역에서 "아이 나이" 표시 제거.
   (기록 시점 기준이라 실제 읽은 시기와 달라 잘못된 정보)
2. ② 작성자 기록 카드의 나이 표기 앞에 "지금"을 붙일 것.
   예: "아하하리 · 지금 48개월". 띠 표시는 유지.

먼저 현재 상세 페이지에서 아이 나이가 어디에 어떻게 표시되는지 확인하고 알려줘.
이미 없으면 작업할 게 없다고 말할 것.
변경 부분만 보여주고 대기할 것.
```

## 4.3 🧑 확인
기록 저장 성공 후 현재 어디로 리다이렉트하는지 확인해서 알려줘. 이 작업순서 작성 시점이라면 /posts/[id]로 보내는게 적절할 것 같은데, 실제 작업하고나서 정해보자.
- [ ] 세그먼트가 보이고 기본값이 '지금 읽고 있어요'
- [ ] 자동 체크된 칩이 1개
- [ ] '예전에 읽었어요' 탭 시 체크가 **전부 풀림**
- [ ] 힌트가 그때만 나옴
- [ ] 아이 없는 계정에서 세그먼트가 숨겨짐
- [ ] ⓘ 문구에 노출 조건이 없음

---

# STEP 5 — 나도 추천해요 + 바텀시트 (예정 기능 D)

## 5.1 🤖

```
'좋아요'를 '나도 추천해요'로 전환해줘.
CLAUDE.md 10.3(바텀시트), 12장, 18.1(신호 3축), 18.2(명칭 규칙) 기준.

1. UI 라벨 '좋아요' → '나도 추천해요', 아이콘 하트 → 👍 엄지.
   DB 테이블명 likes는 그대로 (마이그레이션 하지 말 것).
   대상: src/components/post/PostReactions.tsx, src/components/feed/*
2. 버튼 탭 시 바텀시트를 연다.
   - 제목: "어느 시기 아이에게 추천하시나요?"
   - 6개 시기 칩을 3×2 그리드, 다중 선택
   - 하단 '완료' 버튼으로 확정
   - 기본 선택값을 절대 넣지 말 것 (앵커링 방지)
   - 선택 전에는 분포를 보여주지 말 것. 완료 직후에만 노출.
   - 재탭 시 기존 선택 표시 + 해제 가능
   - 비로그인이면 로그인 유도
3. 완료 시 likes 행에 group_names(text[]) 저장.
   CHECK 제약이 있으므로 한글 라벨만. 폼의 영문 value는 src/lib/groups.ts 매핑으로 변환.
4. 누른 후 표기:
   - 홈 피드 카드: '추천함' (시기명 미표기)
   - 게시물 상세: '추천함 · 봄꽃 +2'
     대표 시기는 선택 순서가 아니라 연령 오름차순 첫 번째로 고정. 시기 1개면 '+N' 없이.
5. 완료 후 분포 표시 — CLAUDE.md 10.3 '분포 표시 형태' 규격 그대로:
   - 세로 막대 6칸, x축 연령 오름차순 고정
   - 단색 + 내가 고른 칸만 진하게 (시기 배지 6색 쓰지 말 것)
   - 절대값 + 총 표 수 표기
   - 5표 미만이면 막대 대신 "아직 N명이 추천했어요"
   - 요약: 최빈 40% 이상 → "OO 시기에 가장 많아요" / 미만 → "여러 시기에 걸쳐 추천돼요"
   - ⚠️ 이 분포 컴포넌트는 STEP 6의 책 페이지에서도 재사용한다.
     src/components/book/PeriodDistribution.tsx 로 분리할 것.
   - 쿼리는 CLAUDE.md 13.3 '주요 쿼리 패턴' (2)번 그대로.

주의:
- 바텀시트는 중앙 모달이 아니라 하단에서 올라오는 형태 (제1원칙).
- 6개 칩이 스크롤 없이 한 화면에 들어와야 함.
- JSX 태그 쌍과 import 누락 사전 검토.

구현 전에 현재 좋아요 관련 파일을 목록으로 보여주고 계획을 말한 뒤 대기할 것.
```

## 5.2 🧑 확인

- [ ] 바텀시트가 하단에서 올라옴
- [ ] 기본 선택된 칩이 **없음**
- [ ] 선택 전에 분포가 **안 보임**
- [ ] 완료 후 분포가 뜸
- [ ] 6개 칩이 스크롤 없이 보임
- [ ] Supabase `likes.group_names` 에 한글 라벨 배열 저장됨
- [ ] 카드 '추천함' / 상세 '추천함 · 봄꽃 +2'
- [ ] 재탭 시 기존 선택 표시·수정됨

## 5.3 🧑 여기서 커밋

```powershell
git add -A
git commit -m "feat: 나도 추천해요 전환, 저장 기능, 기록 폼 개편"
```

STEP 6은 규모가 크므로 반드시 커밋 후 시작한다.

---

# STEP 6 — 책 단위 통합 + 정렬 (예정 기능 G)

## 6.0 🧑 먼저 정할 것

17장 미결 — **`/book/[isbn]` 댓글 위치**

- **(가) 기록별 댓글** — 현행 `comments.post_id` 그대로. 책 페이지엔 댓글 없음 ← **권장**
- (나) 책 단위 스레드 — 책 페이지에 별도 댓글
- (다) 둘 다

(가)를 권하는 이유: 스키마 변경이 없고, 댓글은 특정 기록에 대한 공감이라 책 단위 스레드는 맥락이 흐려진다. 책 페이지의 주역은 분포와 기록 카드 목록이다.

## 6.1 🤖

```
책 단위 통합을 구현해줘. CLAUDE.md 17장(같은 책 통합 노출), 19.2(SEO 구조), 6.2(정렬) 기준.
셋이 서로 맞물려 있으니 한 번에 처리할 것.

[1] 홈 피드 카드를 '기록 1건' → '책 1권' 단위로 묶기
   - src/lib/feed.ts 의 조회를 book_id 기준 집계로 변경
   - 카드 집계값: 추천 N명(합산), 대표 반응(최빈), 글밥량 대표값(최빈 또는 평균)
   - 카드 탭 시 /book/[isbn] 로 이동

[2] 시기 섹션별 정렬 전환
   - src/lib/ranking.ts 를 만들고 RANKING_SWITCH_THRESHOLD = 10 상수
   - 시기별 누적 기록 수 < 10 → 최신순 / ≥ 10 → 추천 수 → 반응 → 최신순
   - 6개 섹션이 서로 다른 정렬 상태일 수 있음. 통일하려 하지 말 것.
   - 기존 '좋아요 많은 순' 코드는 제거

[3] /book/[isbn] 신설
   - Server Component로 SSR. useEffect + 클라이언트 Supabase 호출 금지 (19.2)
   - slug는 ISBN13 (books.aladin_item_id)
   - 구성:
     · 책 정보 (표지/제목/저자/출판사/출간일) + 알라딘 링크
     · 추천 시기 분포 — STEP 5에서 만든 PeriodDistribution 재사용. 상시 노출.
     · 이 책의 기록 카드들을 누적 표시 (10.2)
     · 저장 버튼
   - 댓글은 [여기에 6.0에서 고른 안: 가/나/다] 로 처리
   - generateMetadata (19.2-3, 19.2-4):
     title: {책제목} - {시기}({개월범위}) 그림책 추천 | 책육아정원
     {시기}는 분포의 최빈 시기. 표가 5개 미만이면 작성자가 고른 시기 중 가장 어린 것.
     description: 기록 수 + 대표 기록 앞부분

주의:
- 모바일 우선 반응형
- 기존 /posts/[id] 는 그대로 남겨둘 것 (직접 링크·공유 대응)

구현 전에 src/lib/feed.ts 현재 구조를 보여주고 계획을 말한 뒤 대기할 것.
```

## 6.2 🧑 확인

- [ ] 같은 책이 홈 피드에 **한 번만** 뜸
- [ ] 카드 탭 → `/book/[isbn]` 이동
- [ ] 페이지 소스 보기에 책 제목·본문이 HTML에 있음 (SSR)
- [ ] 브라우저 탭 제목이 동적으로 바뀜
- [ ] 분포 막대가 뜸 (5표 미만이면 텍스트)
- [ ] 기록이 적은 시기는 최신순으로 뜸
- [ ] 새로 쓴 기록이 해당 시기 맨 앞에 보임

---

# STEP 7 — 마무리

## 7.1 🤖

```
CLAUDE.md의 15장(현재 프로젝트 상태)과 16장(로드맵)을 실제 완료 상태에 맞춰 갱신하고,
20장 변경 이력에 v1.5로 오늘 완료한 작업을 추가해줘.
완료율(현재 65%)도 다시 산정해서 적을 것.
17장에서 '/book/[isbn] 댓글 위치'를 확정으로 옮길 것.
어떤 항목을 완료로 옮겼는지 목록으로 보고할 것.
```

## 7.2 🧑 커밋

```powershell
git add -A
git commit -m "feat: 책 단위 통합, /book/[isbn], 시기별 정렬 (v1.5)"
```

---

# 이번 작업에서 손대지 않는 것

- 검색 전체화면 오버레이 (18.3-A) — 우선순위 낮음, 7단계
- 사진 실제 업로드 (Storage) — 별도 작업
- SNS 공유 (카카오/링크) — 7단계
- 운영자 태그 관리 — 8단계
- 배포 — 9단계
- 기본 베이스 컬러·타이포그래피 — 미확정 유지

---

# 남은 결정 항목

| 항목 | 언제 | 권고 |
|---|---|---|
| `/book/[isbn]` 댓글 위치 | STEP 6 전 | (가) 기록별 댓글 유지 |
| 저장 수 임계치 5명 | 출시 후 | 상수로 관리 |
| 정렬 전환 임계치 10건 | 출시 후 | 상수로 관리 |
| 추천 3탭 참여율 | 출시 후 | 낮으면 단일 선택 전환 검토 |
