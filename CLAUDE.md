책육아정원 — CLAUDE.md (최종 통합본, 2026.07)
Claude Code가 매 작업 시작 시 이 파일을 먼저 읽고 프로젝트 맥락을 파악할 것. PRD 원문 + 개발 중 확정된 세부 결정사항 + 현재 진행 상태 모두 포함.


⚙️ Claude 행동 규칙 (필수)
답변마다 확신도 10점 만점으로 표기. 6점 이하면 이유 설명.
놓친 사항이 있으면 먼저 알려줄 것 (작업 시작 전 체크).
코드 제공 시 JSX 태그 쌍, import 누락 등 문법 오류를 반드시 사전 검토할 것.
각 단계 완료 후 다음 단계로 자동 진행하지 말고, 완료 보고 후 대기할 것.


1. 서비스 개요
앱명: 책육아정원 (확정) 형태: 그림책 추천 커뮤니티 모바일 웹앱 한 줄 소개: 부모가 아이와 함께 읽은 그림책을 월령·연령에 맞게 기록하는 "책육아 일기"이자, 개인 기록이 쌓여 커뮤니티 추천 플랫폼이 되는 서비스.

핵심 기능 우선순위 (중요):

아이와의 책 기록을 남기는 육아일기 기능 (가장 핵심)
알라딘 Open API를 활용한 책 정보 연동
개인들의 책육아 기록이 쌓여 형성되는 그림책 추천 커뮤니티

⚠️ 이 서비스는 "커뮤니티"가 1차 목적이 아니라 "개인 기록(일기)"이 1차 목적이고, 그 기록들이 모여 커뮤니티가 되는 구조. UI/UX 결정 시 이 우선순위를 기준으로 판단할 것.


2. 기술 스택 & 개발 환경
구분
기술
비고
프론트엔드
Next.js (App Router)
SSR로 OG 태그 / SEO 지원
백엔드 / DB
Supabase
무료 플랜 — DB 500MB, 스토리지 1GB
배포
Vercel
무료 플랜 — 초기 운영비 0원
스타일링
Tailwind CSS
모바일 우선 반응형 필수
책 정보
알라딘 Open API
제목·저자·출판사·링크·절판 여부
소셜 로그인
카카오 로그인
OAuth 2.0 (Supabase Auth 연동)


개발 환경: Windows, VS Code / Cursor, Node.js v24+ 프로젝트 경로: C:\Users\김가현\chaekugajungwon


3. 설계 원칙
제1원칙: 모바일 웹 반응형 UI — 절대 원칙 모든 화면은 모바일 환경을 최우선으로 설계한다. PC 웹에서도 사용 가능하되, 레이아웃과 인터랙션 기준은 항상 모바일이다. 이 원칙은 어떠한 이유로도 예외가 없다.


4. 사용자 및 권한
역할
권한 범위
일반 사용자
본인 게시물·댓글 작성/수정/삭제, 나도 추천해요·저장, 댓글 작성
운영자
일반 사용자 권한 + 타인 게시물·댓글 숨김(모더레이션), 주제 태그 관리, 특정 계정에 운영자 권한 부여
  ※ 모더레이션 구현(2026.08): 별도 관리자 페이지·계정 없이 users.is_operator=true 계정이 일반 화면에서 처리.
    소프트 삭제(hidden_at) — posts·comments에 hidden_at, 숨김은 복구 가능. 조회는 전부 hidden_at is null 필터.
    숨김 버튼은 작성자 본인+운영자에게만 노출. 라벨 '숨김'. API: /api/posts/[id]/hide, 댓글은 comments DELETE(내부 update).
    RLS 운영자 예외: posts/comments update를 is_operator 허용. getIsOperator(lib/supabase-server.ts)로 판별.
    신고/자동 모더레이션은 트래픽 생긴 뒤(MVP 이후).


운영자 전용 관리 페이지는 별도로 만들지 않는다. 권한 부여는 Supabase 관리 콘솔 또는 운영자 계정 내 설정으로 처리한다.


5. 화면 구조 (네비게이션)
하단 탭 바 3개 + 상단 검색 아이콘으로 구성.

탭
아이콘
화면 설명
홈 피드
홈 아이콘
시기별 탐색 메인 화면
기록하기
플러스 아이콘
새 기록 작성 폼 (/recommend/create)
마이페이지
사용자 아이콘
프로필, 아이 정보, 통계, 책육아 기록
검색
돋보기 아이콘 (상단 고정)
책 제목 입력 → 관련 기록 노출



6. 홈 피드 상세 스펙
6.1 전체 구조
상단 고정 헤더: 앱 로고 + 검색 아이콘
전체 주제 태그 필터 바: 운영자가 설정한 태그 가로 스크롤 칩
시기 섹션 × 6개 (씨앗/새싹/꽃잎/열매/나무/어른) 세로 배치
6.2 시기 섹션 구조 (각 섹션 동일 패턴)
섹션 헤더: 시기 이름 배지 + 연령 설명 + 더보기 버튼
서브 태그 필터 바: 해당 시기에 속한 운영자 태그 가로 스크롤 (밀리의서재 탭 방식 참고)
카드 가로 스크롤 목록: 기본 3개 노출 → 옆으로 스크롤 시 최대 12개
더보기 버튼 → 해당 시기 전체 리스트 페이지로 이동
UI 레퍼런스: TripAdvisor 홈 피드 구조 참고

정렬 기준 (시기 섹션별로 독립 판정):
  - 해당 시기의 누적 기록 수 < 10건 → 최신순
  - 해당 시기의 누적 기록 수 ≥ 10건 → 추천순
      1차: '나도 추천해요' 수 (많은 순)
      2차: 동점이면 우리 아이 반응 (3 > 2)
      3차: 그래도 동점이면 최신순
  ※ 시기마다 누적 속도가 다르므로 6개 섹션의 정렬 상태가 서로 다를 수 있다. 정상이다.
  ※ 상수 RANKING_SWITCH_THRESHOLD = 10 을 src/lib/ranking.ts 에 둘 것.
  ※ 이유: 초기에는 추천 수가 대부분 0이라 0끼리 정렬하면 사실상 무작위가 되고,
    새 기록이 노출되지 않아 작성자가 이탈한다.
  ⚠️ 카드 단위가 '기록 1건'에서 '책 1권'으로 바뀌면(17장) 집계 기준도 함께 바뀐다.
    책 단위 묶기와 정렬 로직은 반드시 같은 작업에서 처리할 것. 따로 하면 두 번 짜게 된다.
6.3 홈 피드 카드에 표시되는 정보
정보 항목
표시 방식
책 표지 이미지
알라딘 API 이미지 or 색상 플레이스홀더
책 제목
최대 2줄
시기
컬러 배지 (7장 6색)
주제 태그 (해시태그)
운영자 태그 최대 2개
글밥량
홈 피드에는 표시하지 않음 (2026.08 확정 — 정보 과밀 방지. 책 페이지 책 정보 영역에서 대표값(최빈) 확인)
우리 아이 반응
이모지+짧은라벨(🥰 자꾸 꺼내봐요 / 😄 재밌어했어요) — 대표 반응(최빈값), 제목 아래 캡션. 기록 수·추천 수·저장은 하단에 아이콘으로(📄기록수 👍추천수 🔖저장 — '기록 N' 텍스트 대신 아이콘 통일). 정보 우선순위: 아이 반응 > 기록 수·주제 > 추천 수·저장 (18장 참조)
나도 추천해요
👍 아이콘 + 카운트 ← 홈 피드 정렬 기준. 누른 후 표기는 '추천함'(카드에서는 시기명 미표기). 카드가 가로 스크롤 안이라 폭이 좁으므로 짧게 유지.
저장 버튼
아이콘 버튼. '나중에 아이와 읽기' 개인 리스트에 추가
저장 수
5명 이상일 때만 'N명이 담았어요'. 미만이면 영역 자체를 렌더링하지 않음(0도 표시 금지)


카드 탭 시 게시물 상세 페이지로 이동.

📌 '나도 추천해요'와 '저장'은 별개 장치다. 하나의 랭킹 축으로 합치지 말 것. (18.1 참조)


7. 시기 기준 (연령 + 색상, 확정)
시기
연령 범위
컬러(hex)
컬러명
씨앗
0~12개월
#A47551
흙빛 브라운
새싹
12~18개월
#A6D879
밝은 연두 (돌 전후 아기 상징, 밝고 산뜻한 톤)
꽃잎
19~30개월
#E2A0B5
분홍 블라썸
열매
31~48개월 (만 4살까지)
#D9614A
사과 코랄
나무
49개월(5살)~
#4F6B43
짙은 숲그린
어른
어른도 즐길 수 있는 그림책
#6B3347
깊은 보르도 (와인톤)


기록 작성 시 시기는 다중 선택 버튼으로 복수 선택 가능.

📌 기본 컬러(메인/포인트/배경/텍스트)는 아직 미확정. 임시: 모스그린 #6B7F5E(메인) / 살구코랄 #E08F5B(포인트) / 한지 아이보리 #FBF6EC(배경) / 잉크브라운 #3A332B(텍스트) 확정 전까지 재논의 가능. Tailwind 등록 시 "확정 아님" 주석 필수.

📌 용어: 화면에 보이는 말은 '시기', 코드·DB·스키마는 'group'을 쓴다. (18.2 참조)

📌 시기는 순서형 변수 — 여러 시기를 나열할 때는 항상 연령 오름차순(씨앗→새싹→꽃잎→열매→나무→어른).
  카드/상세/책 페이지/마이페이지의 시기 배지, 분포 x축, '추천함 · 🌸 +N'의 대표 시기 모두 동일.
  구현: lib/groups.ts의 sortGroupLabelsByAge / GROUP_LABELS_ORDERED (단일 소스).


8. 태그 시스템
8.1 운영자 태그
운영자가 직접 생성 및 관리
홈 피드 상단 필터 바 및 각 시기의 서브 필터에 표시
태그 클릭 시 해당 태그가 붙은 기록만 필터링 (노션 장르별 보드 방식)
태그 비활성화는 삭제 대신 is_active 필드로 처리 (히스토리 데이터 보존)
Supabase에 시드 데이터로 이미 등록 완료

운영자 태그 목록 (확정, 23개): 가족, 친구, 계절, 명절/기념일, 인성/감정/회복탄력성, 다양성, 공룡, 동물, 식물, 곤충, 음식, 생활습관, 색깔, 잠자리독서, 똥/방귀, 말놀이/수놀이, 몸/신체, 기관, 탈것, 캐릭터, 보드북, 조작북, 병풍/팝업책

📌 시기(연령)와 주제 태그(소재/형태)는 별개의 분류축. 한 기록에 동시에 붙일 수 있음. 예: "열매" 시기 + "공룡" 태그 + "보드북" 태그.
8.2 사용자 추가 태그
기록 작성 시 자유롭게 추가 가능
피드와 상세에 해시태그 형태로 표시
필터링·분류에는 적용되지 않음 (표시 전용)


9. 게시물 입력 필드 스펙
필드
타입
설명
책 정보
알라딘 API 검색
제목, 저자, 출판사, 링크, 절판 여부 자동 입력
시기 (라벨: '이 시기 아이가 읽으면 좋아요')
세그먼트 버튼 + 다중 선택 칩
아래 9.2 참조
아이 나이 태그
입력 보조 전용 (저장·표시하지 않음)
프로필 연동으로 현재 월령을 계산해 위 '시기'의 자동 체크에만 사용. ⚠️ posts에 나이를 저장하거나 상세 페이지에 "이 나이에 읽었다"로 표시하지 말 것. 기록 시점과 실제 읽은 시점이 다를 수 있어 잘못된 데이터가 된다. (10.1 참조) post_children 테이블은 유지하되 현재 미사용.
우리 아이 반응 (재독 여부 대체)
3단계 선택 버튼
자꾸 꺼내봐요(🥰,3) / 재밌어했어요(😄,2) / 그냥 봤어요(🙂,1). 옆에 ⓘ 아이콘 → "솔직한 반응이 다른 양육자들에게 도움이 돼요". ⚠️ ⓘ 문구에 피드 노출 조건(2 이상만 노출)을 절대 표기하지 말 것. 노출 조건을 알리면 사용자가 노출을 위해 2 이상을 선택하게 되어 반응 데이터가 인플레이션되고, 월령별 반응 분포 분석이 불가능해진다. (18.1 차별점의 근간)
글밥량
5단계 선택
아래 기준표 참조
주제 태그
칩 선택 + 직접 입력
운영자 태그 선택 + 사용자 추가 태그 입력 가능
책육아 일기
자유 텍스트
제한 없는 자유 메모란
사진
이미지 업로드
최대 3장 (용량 제한)

9.1 글밥량 5단계 기준 (정확히 이대로 구현)
단계
그림:글 비율
기준
★☆☆☆☆
그림 90% / 글 10%
페이지당 1~2단어
★★☆☆☆
그림 80% / 글 20%
한 페이지 1문장 내외
★★★☆☆
그림 70% / 글 30%
한 페이지 2~3문장
★★★★☆
그림 60% / 글 40%
페이지당 여러 문장
★★★★★
그림 50% 이하 / 글 50% 이상
글 많은 편

9.2 시기 선택 UI (2026.07.31 확정, 같은 날 아이콘 칩 + 토글로 개정)

  이 시기 아이가 읽으면 좋아요
  [🐯 47개월✓]  ㅣ  [예전에 읽었어요]      ← 아이 칩(들) + 구분선 + 예전 칩, 한 줄
  (🫘)     (🌱)     (🌸)     (🍎)      (🌳)   (👩‍👦)
  0~12M  12~18M  19~30M  31~48M(4Y)✓  5Y(49M)~  어른도 함께   ← 원형 아이콘 칩 한 줄(다중 선택)
  ※ 연령 표기 단위: 개월=M, 살=Y (모바일 폭 절약, 2026.08 확정). lib/groups.ts ageLabel이 단일 소스.
  ※ 칩 아래 라벨은 시기명(씨앗 등)이 아니라 연령 범위 — 사용자는 시기 이름의 기준을 모른다.
    자동 체크된 칩의 범위와 아이 나이가 눈으로 대조되므로 "내 아이 시기" 인지도 함께 해결.
    라벨 소스는 lib/groups.ts의 ageLabel (홈 피드 섹션 헤더와 동일 표기, 단일 소스).
  ※ 아이 칩은 1명이어도 표시한다 — 자동 체크의 기준(어느 아이의 지금 나이)을 보여주는 역할.
    '지금 읽고 있어요'는 별도 버튼 없이 기본 상태(아이 칩 선택됨)로 표현한다.

■ 동작 보충 (한 줄 구성)
  - 아이 칩 탭 = 그 아이의 현재 월령 기준으로 자동 체크 1개 (기본 상태)
  - '예전에 읽었어요' 탭 = 체크 전부 해제 + 힌트. 재탭 또는 아이 칩 탭으로 복귀.

■ UI 개정 이력 (2026.07.31 당일 반복 끝에 확정)
  1) 대형 세그먼트 2버튼 → 컨트롤 3층(아이+세그먼트+칩)이라 선택 피로 큼.
  2) 제목 우측 토글 칩 1개 → 칩과 줄이 떨어져 무엇의 갈래인지 헷갈림.
  3) 칩 위 소형 2택 칩(지금/예전) → 줄이 하나 더 생김.
  4) 확정: [아이 칩들 ㅣ 예전에 읽었어요] 한 줄 — '지금'은 라벨 없는 기본 상태로 흡수,
     아이 칩이 자동 체크의 기준을 겸해서 설명 캡션 없이도 맥락 전달.
  '예전에 읽었어요'를 제거하지 않는 이유: 연령 범위 라벨로 어긋남을 알아차리기 쉬워졌어도
  사용자는 기본값을 그대로 수용하는 경향이 있어(특히 옛날 책 몰아서 기록하는 초기 사용자),
  자동 체크를 한 번에 해제하는 장치가 없으면 시기 데이터가 현재 월령 쪽으로 밀린다(체계적 편향).

■ 동작
  - 토글 기본값 = 꺼짐(지금 읽고 있어요)
    → 프로필 아이의 현재 월령에 해당하는 칩 1개를 자동 체크. 일반 경로에서 추가 탭 0회.
    → 월령→시기 매핑: <12M 씨앗 / 12~18 새싹 / 19~30 꽃잎 / 31~48 열매 / 49+ 나무 (만4살=한국나이 5살 혼동 방지, 2026.08 조정)
      (lib/age.ts getMonths + lib/groups.ts getGroupValueByMonths)
  - '예전에 읽었어요' 토글 켜면
    → 칩 체크를 전부 해제한다. (앵커 제거가 목적이므로 반드시 해제)
    → 칩 아래에 한 줄 힌트: "그때 시기를 골라주세요"
    → 이 힌트는 토글이 켜져 있을 때만 노출. 평소엔 렌더링하지 않는다.
    → 다시 끄면 자동 체크 1개로 복귀.
  - 칩은 토글과 무관하게 언제든 다중 선택·해제 가능.
  - 자동 체크는 반드시 1개만. 나머지는 사용자 판단으로 남긴다.
  - 아이가 2명 이상이면 아이 탭(띠 이모지+나이)을 먼저 선택 → 해당 아이 기준으로 자동 체크.
  - 아이 정보가 없는 사용자는 토글·아이 탭을 숨기고 빈 칩만 노출.
  - 읽은 시점(지금/예전)은 저장하지 않는다 — 자동 체크 유효성 판단 전용.

■ 검증
  - 칩 미선택 상태로는 제출 불가. 인라인 에러.

■ ⚠️ 설계 근거 — 아래를 바꾸지 말 것
  - 캡션(설명 문구)으로 자동 선택을 안내하지 말 것.
    사용자는 폼에서 '조작할 것'을 찾지 '읽을 것'을 찾지 않으며, 회색 보조 문구는 건너뛴다.
    같은 정보를 글자가 아니라 컨트롤(토글 칩)로 표현해야 인지된다.
  - 모달·알림창·바텀시트를 쓰지 말 것.
    기록 폼은 이미 사용자가 기록 의도로 진입해 필드를 순서대로 채우는 맥락 안에 있다.
    오버레이를 띄우면 안내가 아니라 흐름 차단이며, 시트는 열어야 보이므로 선택지까지 숨는다.
    ※ 추천 바텀시트(10.3)는 카드 버튼을 눌렀을 뿐 아무 맥락이 없는 상태라 성격이 다르다.
  - 기록 작성은 주 행동, 추천 투표는 부 행동이다. 마찰은 투표 쪽에 두고 기록 쪽에는 두지 않는다.


10. 게시물 상세 페이지
10.1 영역 구분 (3개 영역)
① 책 정보 영역

책 표지 이미지 / 책 제목 / 저자 / 출판사·출간일
더보기 버튼 → 알라딘 해당 상품 링크로 이동
⚠️ "아이 나이(작성 시 태그된 아이 나이)" 항목은 표시하지 않는다.
   기록 시점 기준이라 실제 읽은 시기와 다를 수 있어 잘못된 정보다.

② 작성자 기록 카드 영역

작성자 닉네임만 표시. 아이 띠·나이는 표시하지 않는다 (2026.07.31 확정)
  ※ 이유: 중요한 정보는 "기록 시점의 아이 나이"가 아니라 작성자가 고른 추천 시기이며,
    시기 배지가 그 역할을 한다. 아이가 여럿이면 어느 아이인지 고르는 문제도 생기고,
    타인이 아이 정보(children)를 읽으려면 RLS 완화(생년월일 공개)가 필요해 개인정보상 부적절.
시기 배지 / 주제 태그(운영자 태그 + 작성자 추가 태그)
글밥량(별점) / 우리 아이 반응(이모지+라벨) / 책육아 일기(자유 메모) / 사진(최대 3장)

③ 반응 영역

👍 나도 추천해요 버튼 → 바텀시트로 추천 시기 선택 (10.3 참조)
  누른 후 표기: '추천함 · 꽃잎 +2'
    · 대표 시기는 연령 오름차순 첫 번째로 고정한다.
      (선택 순서 기준이면 같은 조합인데 사람마다 표기가 달라진다.
       또한 그림책은 가장 어린 시기가 정보량이 가장 크다.)
    · 시기 1개면 '추천함 · 꽃잎', 2개 이상이면 '추천함 · 꽃잎 +N'
저장 버튼 (책 단위)
댓글 창 (감상평 및 후기 작성)
10.2 같은 책 다수 기록 처리 (/book/[isbn] 구현 완료, 2026.08 기준 확정)
동일 책에 여러 사용자가 기록을 작성한 경우, 책 정보 영역은 하나로 공유하고 그 아래에 기록 카드를 작성순으로 누적 표시.
■ 정보 배치 기준 (2026.08 확정)
  - 책 정보 영역 = 책의 객관 정보: 표지·제목·저자·출판·절판·알라딘 링크 + 글밥량 대표값(최빈) + 저장 버튼
  - 기록 카드 = 양육자의 평가 전용: 닉네임·시기 배지·우리 아이 반응·책육아 일기(3줄)
    글밥량은 기록 카드에 표시하지 않는다(책 객관 정보이므로 위로 이동).
  - '나도 추천해요'(👍)는 책 정보 영역(저장 버튼 옆)에서 누른다(바텀시트) — 책·시기 판단이므로.
    기록 카드에는 두지 않는다(2026.08 확정 — '후기 도움돼요'로 오독됨).
    세부 기록 페이지(/posts/[id])는 댓글 진입·공유 링크용으로 유지.

10.3 '나도 추천해요' 바텀시트 (2026.07.31 신설)

■ 왜 시기를 묻는가
버튼을 누른 시점의 아이 월령을 자동으로 붙이면 실제로 읽었던 시기와 어긋난다.
48개월 아이의 부모가 돌 무렵 읽었던 책에 추천하면 48개월 데이터로 기록된다.
오래된 책일수록 오차가 커지고, 하필 그런 책이 좋은 책이다.
무작위 노이즈가 아니라 체계적 편향(모든 책이 실제보다 높은 월령으로 밀림)이므로
표본이 커져도 상쇄되지 않는다. 따라서 시기는 반드시 사용자에게 직접 묻는다.

■ UI 형태: 바텀시트
  - 네이티브 alert/confirm: 선택지를 넣을 수 없음. 사용 금지.
  - 중앙 모달: 화면 중앙이라 엄지가 닿지 않음. 제1원칙(모바일 우선) 미달.
  - 인라인 확장: 피드 카드가 가로 스크롤 안이라 폭이 좁고 레이아웃이 밀림. 부적합.
  - 바텀시트: 하단에서 올라와 엄지 영역에 들어옴. 모바일 표준. ✅

■ 시트 구성
  - 제목: "어느 시기 아이에게 추천하시나요?"
  - 6개 시기 칩을 3×2 그리드 (스크롤 없이 한 화면)
  - 다중 선택 → 하단 '완료' 버튼으로 확정
  - 재탭 시 현재 선택 상태 표시 + 해제 가능
  - 비로그인 시 로그인 유도

■ ⚠️ 분포는 '완료' 이후에만 보여줄 것
  선택 전에 "꽃잎 40표"를 보여주면 대부분 꽃잎을 누른다.
  분포가 최빈값으로 수렴해 이 데이터의 유일한 가치인 분산이 사라진다.
  완료 직후에 보여주면 앵커링 없이 정보 보상만 남는다.
  ("내가 고른 게 다수와 같네/다르네"가 더 강한 보상으로 작동한다)

■ ⚠️ 기본값·프리셋을 두지 말 것
  작성자가 고른 시기나 현재 아이 월령을 미리 선택해두면 앵커링이 걸린다.
  이 데이터의 유일한 가치는 작성자 판단과 독립적이라는 점이다.
  ※ 기록 폼(9.2)은 자동 체크를 하지만 그것은 본인의 기록이다.
    투표는 타인의 기록에 대한 독립 판단이므로 반드시 빈 상태에서 시작한다.

■ 분포 표시 형태 (완료 후 / 책 페이지 공통 컴포넌트)
  - 세로 막대 6칸. x축은 씨앗→어른 연령 오름차순 고정.
    이유: 시기는 순서형 변수라 x축 순서가 곧 정보다. 가로 막대 목록은 그 순서를 버린다.
          또 "한 시기에 뾰족한가 / 여러 시기에 퍼졌는가"라는 분포의 모양이 한눈에 읽힌다.
          시기명이 전부 2글자라 6칸 라벨이 모바일 폭에 들어간다.
  - 색은 단색. 시기 배지 6색을 쓰지 말 것 — 색이 6개면 높이보다 색을 먼저 읽어 모양이 안 보인다.
    내가 고른 칸만 진하게, 나머지는 흐리게.
  - 비율이 아니라 절대값. 총 표 수를 함께 표기 (3표짜리와 300표짜리가 구분되어야 함).
  - 5표 미만이면 막대를 그리지 말 것. "아직 N명이라 시기 분포를 보여주기엔 일러요" 텍스트로 대체.
    (0명이면 "아직 추천 데이터가 없어요") 2표로 그린 막대는 정보가 아니라 오해다.
    ※ 분포 모수에는 기록자(post_groups)도 포함되므로, 기록이 있으면 최소 1명 이상이다.
  - 요약 문장:
      최빈 시기가 전체의 40% 이상 → "꽃잎 시기에 가장 많아요"
      40% 미만 → "여러 시기에 걸쳐 추천돼요"
    ※ 이 로직은 책 페이지 generateMetadata의 연령그룹 값과 동일하게 쓴다. 한 번 만들어 두 곳에서 사용.

■ 비용 인지
  다중 선택이므로 완료 버튼이 필수 → 총 3탭 (버튼 → 시기 → 완료).
  기록 폼과의 일관성을 위해 다중 선택을 채택했다.
  참여율이 단순 좋아요보다 낮아질 수 있다. 출시 후 측정할 것.

■ 앵커링의 한계 (알고 갈 것)
  책 페이지에는 분포가 상시 노출되므로, 그 페이지를 거쳐 들어온 사용자는
  이미 분포를 본 상태로 투표하게 된다. 이건 막지 않는다.
  - 책 페이지의 존재 이유가 분포를 보여주는 것이다.
  - 외부 SEO 유입자는 정보를 보러 온 사람이지 투표하러 온 사람이 아니며, 대개 비로그인이다.
  - 나중에 그 사용자가 실제로 아이와 읽으면 그때는 본인 기준이 생긴다.
  홈 피드 경로(다수)만 지키면 충분하다.

■ 이 데이터가 만드는 것
  1) 책 단위 "추천 시기 분포" → /book/[isbn]의 핵심 콘텐츠
  2) 집단이 기록의 시기 배치를 검증 (작성자 1명의 판단 → N명의 집계)
     예) 작성자는 '꽃잎'으로 올렸으나 투표는 새싹 12 / 꽃잎 40 / 열매 8
  3) 기록을 쓰지 않는 다수 사용자로부터 월령 데이터를 확보


11. 프로필 스펙
11.1 기본 정보
닉네임: 카카오 이름 기본값, 사용자가 직접 수정 가능
로그인 수단: 카카오 로그인 (OAuth 2.0, Supabase Auth 연동)
11.2 아이 정보
아이 생년월일 다수 등록 가능
십이지신 아이콘으로 시각적 구분
나이 자동 표기 로직 (정확히 이대로 구현):

개월 수
표기
36개월 미만
OO개월
36~47개월
만 3살
48~59개월
만 4살
60~71개월
만 5살
72개월 이상
초등학생 이상

11.3 마이페이지 구성 (2026.07.31 확정·구현 완료)
프로필 (닉네임 수정, 아이 정보 관리)
독서 통계: 함께 읽은 책(중복 제외) + 책육아 기록 수
책육아 기록을 두 목록으로 분리:
  ① '아이와 함께 읽은 책' — 내가 기록(posts)한 것
  ② '아이와 함께 읽고 싶은 책' (제목 옆 저장 아이콘) — 저장(bookmarks)한 것
  ※ 저장해 둔 책을 기록하면 bookmarks에서 자동 삭제되어 ②에서 빠진다.
    (저장 모집단 = '아직 안 읽은 사람' 유지. recommend API가 기록 성공 시 자동 해제)
  ※ ② 리스트에서 저장 버튼을 해제해도 행은 그대로 남는다(실수 방지 — 다시 눌러 복구 가능).
    목록에서 실제로 빠지는 건 새로고침(재조회) 시점. 즉시 제거하지 말 것.


11.4 첫 기록 게이트 (2026.08 확정)
이 서비스의 핵심은 추천 기록이 쌓이는 것 — 모든 유저는 기록 1건을 쓰고 서비스를 시작한다.
  - 흐름: 카카오 가입 → 온보딩(닉네임·아이) → 첫 기록 화면(/recommend/create?first=1) → 기록 완료 → 홈 입장
  - 가드: 기록(posts) 0건인 로그인 유저가 홈(/)·검색(/search)에 접근하면 첫 기록 화면으로 리다이렉트.
    콘텐츠 소비 입구만 막는다 — 마이페이지·기록하기는 자유. 기존 유저(기록 보유)는 영향 없음.
  - 첫 기록 모드 UI: 웰컴 배너("책육아정원은 기록으로 시작해요"), 홈 링크 숨김,
    버튼 '첫 기록 남기고 시작하기', 완료 후 상세가 아니라 홈(피드)으로 이동.
  - 건너뛰기 없음(확정). 출시 후 '가입→첫 기록 완료율'을 측정해 낮으면 완화 검토(17장).


12. 공통 기능
기능
설명
카카오 로그인
OAuth 2.0 기반 소셜 로그인
SNS 공유
책·기록 페이지 공유(2026.08 구현). ShareButton: Web Share API(모바일 네이티브 시트) → 미지원 시 링크 복사 폴백. OG/트위터 태그를 generateMetadata에 추가(제목·설명·표지 이미지) → 카톡·SNS에 표지 카드 미리보기. 카카오 전용 공유 버튼(JS SDK)은 키 확보 시 추가.
나도 추천해요 (구 좋아요)
👍 엄지 아이콘. 탭 시 바텀시트로 추천 시기 다중 선택 → 완료. 재탭 시 수정·해제 가능. 홈 피드 정렬의 1차 기준. 저장 단위는 책(likes.book_id, 2026.08 전환) — "책·시기에 대한 판단"이므로 책 레벨이 정합. 버튼 위치: 책 페이지·상세의 책 정보/반응 영역(기록 카드 아님 — 기록 카드에 두면 '후기 도움돼요'로 오독). 표기: 카드 '추천함' / 상세 '추천함 · 🌸 +2'(대표 시기 아이콘)
저장 (북마크)
'아이와 함께 읽고 싶은 책' 개인 리스트. 마이페이지 ②목록의 소스. 저장 단위는 게시물이 아니라 책(book_id) — 같은 책의 다른 기록을 저장해도 리스트에 1권만 뜬다. 랭킹·정렬에 사용하지 않는다(모집단이 '아직 안 읽은 사람'이라 추천과 배타적). 그 책을 기록하면 자동으로 저장 해제되고, 이미 기록한 책은 저장할 수 없다(409 "이미 아이와 함께 읽은 책이에요") — 양방향 가드로 ①·② 목록의 모순을 방지. 저장 수 노출 조건 3가지: (1) 숫자만 공개, 명단 절대 비공개 (2) 5명 이상일 때만 표시, 미만이면 영역 자체를 렌더링하지 않음(0도 금지). 상수 BOOKMARK_COUNT_MIN_DISPLAY = 5 (3) 랭킹·정렬 미사용. 운영 지표로서 "저장은 많은데 기록이 없는 책" = 콘텐츠를 어디에 채울지 알려주는 내부 신호.
댓글
감상평·후기 작성. 작성자는 본인 댓글만 삭제 가능
검색
책 제목 또는 작가명 입력 → 두 섹션 구조 (2026.08 확정. 작가 검색: 알라딘 QueryType=Keyword + search_books의 author 매칭).
  ① '책육아 기록이 있는 책' — 우리 DB. search_books 함수(supabase/add_search_function.sql): 띄어쓰기 무시 부분 일치('달님안녕'↔'달님 안녕') + pg_trgm 유사도. 기록 수→추천 수 정렬, 탭하면 /book/[isbn].
  ② '아직 기록이 없는 책' — 알라딘 검색 결과 중 DB에 없는 책(최대 10). 정렬: 제목 정확 일치(공백 무시)→접두 일치→국내 우선→세일즈포인트. 점선 테두리+'책육아 기록 없음' 라벨로 기록 없는 책임을 명확히 구분. '첫 기록 남기기' 버튼 → 기록 폼에 책 제목 프리필(?query=)되어 즉시 자동 검색.
  ①이 비어도 ②로 기록 유도 — 모든 검색이 발견 아니면 기록 유도로 끝난다(콜드스타트 전략).
  기록 폼 검색 최소 길이 1(한 글자 제목 '점' 대응). ※ 한 글자 오타 매칭은 한글 trigram 특성상 한계
신고 기능
⏸ MVP 이후 추가 예정



13. 외부 API 연동 스펙
13.1 알라딘 Open API (2026.07.31 갱신)
사용 API: 상품 검색 API (ItemSearch.aspx)만 사용
공통 파라미터: QueryType=Keyword(제목+저자 통합, 작가명 검색 지원) / Cover=Mid / Output=JS / Version=20131101

검색 대상은 국내도서 + 외국도서 두 갈래를 병렬 호출한다.

[A] 국내도서 (SearchTarget=Book)
  - CATEGORY_IDS = [1108, 13789, 2030]
    (1108=유아>4~7세>그림책, 13789=유아>0~3세>그림책, 2030=추가 그림책 분류)
  - ItemSearch는 CategoryId를 하나만 받으므로 CID별 병렬 호출(Promise.all). CID당 MaxResults=30.
  - CID 추가는 배열에 숫자만 추가.
  - ⚠️ 106165는 국내도서가 아니라 외국도서 트리의 CID다. 기존에 CATEGORY_IDS에 들어 있었으나
    SearchTarget=Book으로는 도달할 수 없어 빈 응답을 받고 있었다. [B]로 이동했다.
  - ※ 이전 문서의 "외국도서(번역 그림책) 제외" 표현은 부정확했다.
    알라딘 기준 한국 출판사가 발행한 번역 그림책(달님 안녕, 배고픈 애벌레 등)은 '국내도서'다.
    실제로 빠지던 것은 영어 원서이며, 그것은 [B]로 해결한다.

[B] 외국도서 / 영어 원서 (SearchTarget=Foreign)  ← 2026.07.31 추가
  - 알라딘은 국내도서와 외국도서가 별도 카테고리 트리다.
    국내도서 CID로는 원서에 도달할 수 없으므로 SearchTarget을 분리해야 한다.
  - 목적: 영어책육아 수요(ORT, Karen Katz, Eric Carle 원서 등) 대응.
  - FOREIGN_CATEGORY_IDS = [106165]   (외국도서 > 어린이)
  - MaxResults=10 (국내도서보다 낮게 두어 노이즈 억제)

[C] 병합
  - [A]+[B]를 Promise.all로 모아 isbn13(없으면 link) 기준 중복 제거.
  - 정렬: 국내도서 우선, 그 뒤 외국도서. (주 수요가 국내도서)
  - 한쪽 호출이 실패해도 다른 쪽 결과는 반환할 것.

mallType 필터: 응답에서 BOOK(국내도서)·FOREIGN(외국도서)만 사용, EBOOK/DVD/음반/중고 제외.
  ※ 기존에는 CID가 국내도서 트리 전용이라 FOREIGN이 결과에 등장할 수 없어 죽은 조건이었다.
    [B] 추가로 비로소 실제 동작한다.
절판 여부: stockstatus !== '' && stockstatus != null → isOutOfPrint: true
검색 UX: 기록 폼은 입력 300ms 디바운스 후 자동 검색 → 자동완성 드롭다운(표지+제목)으로 선택.
  (예정 개편: 드롭다운 → 전체화면 검색 오버레이. 폼 상태 보존 위해 별도 라우트가 아닌 같은 페이지 오버레이 권장. 18.3-A)
구현 위치: src/app/api/aladin/search/route.ts
환경변수: .env.local의 ALADIN_TTB_KEY
13.2 카카오 로그인 (Supabase Auth 연동)
scope: profile_nickname profile_image만 명시 (account_email 제외)
PKCE 쿠키는 NextResponse.redirect()에 수동으로 배열 수집 후 적용
Supabase URL Configuration에 등록된 Redirect URLs:
http://localhost:3000/auth/callback
http://localhost:3000/**
Server Component의 setAll 쿠키 핸들러는 반드시 try/catch로 감쌀 것

13.3 Supabase DB 스키마 & 저장 파이프라인 (2026.07 확정)
⚠️ 게시물은 단일 테이블이 아니라 정규화 구조다. (초기 코드가 존재하지 않는 'recommendations' 테이블에 저장하려다 실패 → 아래 구조로 수정 완료)
주요 테이블/컬럼:
  - users: id(uuid, auth.users와 동일), kakao_id(varchar NOT NULL), nickname, is_operator, created_at
  - children: id, user_id, label(기본 '아이'), birth_date, created_at
  - books: id, aladin_item_id(varchar NOT NULL, 실제로는 ISBN13 저장), title, author, publisher,
           published_date(date), cover_image_url, aladin_url, is_out_of_print, fetched_at,
           bookmark_count(integer NOT NULL DEFAULT 0)  ← 2026.07.31 추가 (트리거 자동 동기화)
  - posts: id, user_id, book_id(→books), reread(bool), text_density(smallint 1~5), content(메모), created_at, updated_at
  - post_groups: post_id, group_name  ← CHECK 제약: 한글 라벨('씨앗'/'새싹'/'꽃잎'/'열매'/'나무'/'어른')만 허용
  - post_tags: post_id, tag_id(→operator_tags, nullable), custom_tag(nullable), is_operator_tag
               ← CHECK 제약(chk_tag_type): 운영자태그면 tag_id 있고 custom_tag NULL / 커스텀이면 반대
  - post_images: post_id, image_url, sort_order
  - post_children: post_id, child_id
  - operator_tags: id, name, tag_category, is_active, created_by, created_at
  - likes: user_id, book_id(→books), group_names(text[] NOT NULL DEFAULT '{}'), created_at
           ← 2026.08 변경 (구: post_id). PK(user_id, book_id) — user당 책 1행 = 행 수가 곧 추천자 수.
           ※ UI 라벨은 '나도 추천해요'. 테이블명 likes는 유지.
           ※ 책 단위로 전환한 이유: 추천 = "책·시기에 대한 판단"이라 책 레벨이 정합(17장 👍 의미 충돌 해소).
             기록 간 랭킹은 미사용(홈=책 단위, 책 페이지=작성순)이라 손실 없음.
             기록 공감('도움돼요')이 필요해지면 출시 후 별도 장치로 신설.
           ※ group_names = 이 사용자가 추천하는 시기 배열. 다중 선택.
             CHECK 제약으로 6개 한글 라벨만 허용(post_groups와 동일).
           ※ 레거시 행은 group_names = '{}' 로 남고 시기 집계에서 자동 제외된다.
  - bookmarks: user_id, book_id(→books), created_at   ← 2026.07.31 변경 (구: post_id)
           ※ 저장 단위는 책. UNIQUE(user_id, book_id). 19.2의 /book/[isbn] 구조와 일관.
  - comments: id, post_id, user_id, content, created_at
기록 저장 순서(src/app/api/recommend/route.ts):
  books(aladin_item_id=ISBN13로 있으면 재사용/없으면 insert) → users self-heal(upsert) → posts
  → post_groups(시기 value를 한글 label로 변환) → post_tags(운영자태그는 id 연결). 중간 실패 시 posts 롤백.
시기 저장 방식: post_groups에는 한글 라벨 저장. 폼의 value('seed' 등)는 lib/groups.ts 매핑으로 변환.
사진: post_images에 저장(2026.08 구현). 폼에서 File을 Storage 버킷 post-images/{user_id}/{uuid}.{ext}에 업로드(public URL) → recommend API가 post_images에 sort_order와 함께 insert. 상세 페이지에서 표시. 버킷·RLS: supabase/add_post_images_storage.sql(본인 폴더만 업로드/삭제, 누구나 읽기). 아이 태그(post_children)는 아이 선택 UI 구현 시 연결 예정(현재 미저장).
RLS 정책: supabase/rls_policies.sql 에 정의. "누구나 읽기 / 본인 것만 쓰기(아이·북마크는 본인만 읽기),
          operator_tags 쓰기는 운영자만". 스키마 변경 시 이 파일도 갱신하고 SQL Editor에서 재실행.
users 행 생성: 카카오 콜백에서 kakao_id 포함해 insert. 누락 대비해 recommend/profile 라우트에서 upsert self-heal.

주요 쿼리 패턴 (2026.08 갱신 — likes book_id 전환 반영):
  -- (1) 책 추천자 수 — user당 1행이라 단순 count
  select book_id, count(*) as recommend_count from likes group by book_id;

  -- (2) 책 단위 '추천 시기 분포' (/book/[isbn] 상시)
  --   추천 시기 = ①나도 추천해요(likes.group_names) + ②기록 시 고른 시기(post_groups) 합집합.
  --   기록자도 "이 시기에 추천"한 사람이므로 함께 센다. 유저별 합집합으로 중복 제거(1명=1표).
  --   구현: src/lib/distribution.ts (likes+posts 조회 후 유저별 Set 합집합, JS 집계). recommend API·책 페이지 공용.

  -- (3) 저장 수 — books.bookmark_count 를 그대로 읽는다 (트리거로 동기화, 직접 count 금지)

마이그레이션 파일 (전부 적용 완료):
  supabase/add_child_reaction.sql          — posts.child_reaction
  supabase/add_like_groups.sql             — likes.group_names (가)
  supabase/migrate_bookmarks_to_book.sql   — bookmarks post_id→book_id (나)
  supabase/add_bookmark_counter.sql        — books.bookmark_count + 트리거 (다)
  supabase/rename_groups_flower_fruit.sql  — 시기 명칭 봄꽃→꽃잎, 사과→열매
  supabase/migrate_likes_to_book.sql       — likes post_id→book_id (2026.08, 추천의 책 단위 전환)
  supabase/add_search_function.sql        — search_books 함수 + pg_trgm (책 제목·작가 유사 검색)
  supabase/add_post_images_storage.sql     — 기록 사진용 Storage 버킷 post-images + RLS
  supabase/add_moderation_hidden.sql       — posts·comments hidden_at(소프트 삭제) + 운영자 RLS 예외
  supabase/seed_sample.sql / cleanup_sample.sql — 확인용 샘플 (⚠️ 배포 전 cleanup 실행 필수)


14. 디자인 시스템
14.1 디자인 무드 (중요)
키워드: 따뜻하고 포근하며 맑고 깔끔하고 자연스러운 느낌
톤앤매너: 그림책 종이 같은 감성. 육아일기처럼 부드럽고 기록하는 느낌.
금지 톤: 유아용 키즈 앱처럼 원색 과다·과도한 활기참·장난감 느낌 NO
배경: 흰색 아닌 따뜻한 아이보리(한지 느낌) 계열
14.2 UI 레퍼런스
홈 피드 전체 구조: TripAdvisor 홈 피드 참고
시기별 서브 태그 필터: 밀리의서재 장르 탭 방식 참고 (가로 스크롤 칩)
14.3 폰트 방향 (미확정)
제목/서비스명: 손글씨 느낌의 한글 세리프 (예: 고운바탕)
본문/UI 텍스트: Pretendard 추천
확정 전까지 시스템 기본 폰트로 임시 처리 가능
14.4 확정된 컬러
시기 배지 6색 — 확정 (7번 섹션 참조)
14.5 미확정 컬러
기본 베이스 컬러(메인/포인트/배경/텍스트) — 재논의 필요. Tailwind 등록 시 "확정 아님" 주석 필수.
14.6 Tailwind 적용 방식
globals.css의 @theme 블록 사용 중(v4). 작업 전 Tailwind 버전 재확인할 것.


15. 현재 프로젝트 상태 (2026.08 기준, 약 80%)  ← v1.5, 6.5·6.7단계 완료
완료된 작업
작업
파일
상태
Next.js 세팅 + 디자인 토큰(@theme, 시기 배지 6색)
globals.css
✅
카카오 OAuth 로그인 + 콜백 + 로그아웃 + users self-heal
src/app/api/auth/*, src/app/auth/callback
✅
온보딩(닉네임·아이정보) + 프로필 저장 API
src/app/onboarding, src/app/api/auth/profile
✅
알라딘 검색 API(국내 CID + 외국도서 Foreign 병렬, mallType 필터)
src/app/api/aladin/search/route.ts
✅ (영어 원서 검색 실측 확인)
기록하기 폼(검색 자동완성, 반응 3단계+ⓘ, 시기 아이콘 칩+연령범위, 아이칩ㅣ예전에 자동 체크)
src/app/recommend/create/page.tsx
✅
기록 저장 API(정규화 저장 + 기록 시 저장 자동 해제)
src/app/api/recommend/route.ts
✅
홈 피드(SSR, 책 1권=카드 1개 집계, 시기별 정렬 전환, 태그 필터, 반응≥2만 노출)
src/app/page.tsx, src/lib/feed.ts, src/lib/ranking.ts, src/components/feed/*
✅
책 단위 페이지(/book/[isbn]: 분포 상시·기록 누적·generateMetadata)
src/app/book/[isbn]/page.tsx, src/lib/distribution.ts
✅
게시물 상세(SSR, 3영역, 댓글 진입용)
src/app/posts/[id]/page.tsx
✅
나도 추천해요(책 단위 likes.book_id + 바텀시트 + 분포 컴포넌트)
src/app/api/books/[id]/recommend, src/components/RecommendButton.tsx, post/RecommendSheet.tsx, book/PeriodDistribution.tsx
✅
저장(북마크, book_id 단위 + 양방향 가드 + 카운터 트리거)
src/app/api/books/[id]/bookmark, src/components/BookmarkButton.tsx
✅
댓글 작성/삭제
src/app/api/posts/[id]/comments, src/components/post/PostReactions.tsx
✅
마이페이지(프로필 편집, 통계, ①함께 읽은 책 / ②읽고 싶은 책 2분할)
src/app/mypage, src/components/mypage/*
✅
공통 레이아웃(하단 탭바, 상단 헤더), 아이 나이 자동 계산·갱신
src/components/BottomTabBar.tsx, FeedHeader.tsx, src/lib/age.ts, AgeLabel.tsx
✅
DB 마이그레이션 전체(13.3 목록) + RLS
supabase/*.sql
✅ (Supabase 적용 완료)

미완료 (앞으로 만들 것) — 2026.08 갱신
■ 배포 전 정리 (필수)
  - 샘플 데이터 삭제: supabase/cleanup_sample.sql 실행
  - 개발용 분포 샘플 페이지 삭제: src/app/dev/distribution
■ 남은 기능
  - A: 도서 검색 전체화면 오버레이 (우선순위 낮음)
  - 주제 태그 서브 필터: 홈 피드 각 시기 섹션 서브 태그 필터(6.2 밀리의서재식) + 그룹 전체보기 필터.
    현재 홈 상단 전역 태그 필터(TopicFilterBar)만 있음. 데이터 쌓이면 구현.
  - 사진 실제 업로드 ✅ / 그룹 전체보기 ✅ / 검색(제목·작가) ✅ / SNS 공유·OG ✅ (2026.08)
  - 카카오 전용 공유 버튼 (JS SDK 키 확보 시)
  - 모더레이션(타인 글·댓글 숨김) ✅(2026.08). 운영자 태그 관리 UI(간이) 남음
  - 배포(Vercel) 및 도메인 구매·설정 → 배포 후 모바일 테스트


16. 전체 개발 공정 로드맵
단계
내용
상태
1단계
디자인 토큰 등록 (Tailwind 컬러 등록)
✅ 완료 (globals.css @theme, 시기 배지 6색)
2단계
인증 마무리 (로그아웃 검증, users 테이블 upsert, 닉네임/아이정보 등록)
✅ 완료 (kakao_id 채움, onboarding, RLS, users self-heal)
3단계
기록하기 화면 검증 (Supabase 저장 검증, 검색 UX 최종 확인)
✅ 완료 (정규화 스키마 저장 검증, CID 검색 + 자동완성 드롭다운)
4단계
홈 피드 화면
✅ 완료 (SSR, 월령 6그룹, 태그 필터, 인기순, 반응≥2만 노출)
5단계
게시물 상세 페이지
✅ 완료 (3영역, generateMetadata, 좋아요·댓글)
6단계
마이페이지
✅ 완료 (프로필 편집, 통계, 책육아 기록)
6.5단계
DB 마이그레이션(가·나·다) → 저장(B) → 기록 폼 개편(C·F) → 나도 추천해요(D) → 외국도서(E)
✅ 완료 (2026.08. 시기 명칭 꽃잎/열매 전환 포함)
6.7단계
책 단위 통합: 홈 피드 책 단위 묶기 + /book/[isbn] + 시기별 정렬 전환 로직
✅ 완료 (2026.08. 추천도 책 단위(likes.book_id)로 전환, 👍 의미 충돌 해소)
7단계
공통 기능: 추천✅ 저장✅ 댓글✅ 검색✅ SNS공유·OG✅ / 검색 전체화면(A)·카카오 공유 남음
🔄 대부분 완료
8단계
운영자 기능 (태그 관리, 타인 게시물 삭제)
⏳
9단계
배포 (GitHub → Vercel, 환경변수 이전, 도메인 구매·설정)
⏳
10단계
배포 후 모바일 테스트 및 버그 수정
⏳



17. 미결 / 보류 사항
항목
상태
비고
앱 이름
확정
"책육아정원"
도메인
미구매
bookgardening.com이 유력 후보. 9단계 배포 시 최종 결정
기본 베이스 컬러
재논의 중
시기 배지 컬러만 확정
타이포그래피
미확정
방향만 결정 (세리프+산세리프 페어링)
추천 버튼 아이콘
확정
👍 엄지
저장 수 표시 임계치
잠정 5명
상수 BOOKMARK_COUNT_MIN_DISPLAY. 출시 후 조정
정렬 전환 임계치
시기당 10건
상수 RANKING_SWITCH_THRESHOLD. 출시 후 조정
추천 3탭 참여율
측정 필요
낮으면 단일 선택 전환 재검토
가입→첫 기록 완료율
출시 후 측정
첫 기록 게이트(11.4, 건너뛰기 없음). 완료율이 낮으면 건너뛰기 허용 등 완화 검토
같은 책 통합 노출 (/book/[isbn])
설계 확정·미구현
구현 완료 (2026.08): 책 1권=카드 1개(집계: 추천 N명·대표 반응 최빈·기록 N), 클릭 시 /book/[isbn]에서 책 정보(글밥량 대표값 포함) + 추천 시기 분포 상시 + 기록 카드 작성순 누적.
└ /book/[isbn] 댓글 위치
확정 (2026.08): (가) 기록별 댓글
comments.post_id 그대로, 책 페이지에는 댓글 없음. 이유: 스키마 변경이 없고, 댓글은 특정 기록에 대한 공감이라 책 단위 스레드는 맥락이 흐려진다. /posts/[id]는 댓글 진입·공유 링크용으로 유지.
👍 아이콘의 의미 충돌
확정 (2026.08 해소)
엄지의 관습적 의미("후기 도움돼요"=기록 공감)와 기능("나도 이 책을 이 시기에 추천")이 어긋나던 문제 → 추천을 책 레벨로 이동해 해소: likes를 book_id 단위로 전환(migrate_likes_to_book.sql), 버튼은 책 페이지·상세의 책 정보/반응 영역에만 배치(기록 카드에서 제거). 기록 공감('도움돼요')은 출시 후 기록 정렬이 필요해질 때 별도 장치로 신설 검토.
신고 기능
보류
MVP 이후 추가
검색 범위
확정 (2026.08)
책 제목 + 작가명. 태그 검색은 추후 논의



18. 제품 모델 · 명칭 규칙 · 예정 기능 (2026.07 확정)

18.1 핵심 제품 모델 — "개인은 기록, 커뮤니티(집계)가 추천" (확정, 코드 반영됨)
트립어드바이저식 추천/비추천 이분 마크는 채택하지 않는다.
사용자는 읽은 책을 '기록'하고, 그 기록이 집계되어 커뮤니티 '추천'이 된다.

■ '우리 아이 반응' 3단계 (재독 여부를 흡수)
  - 3 자꾸 꺼내봐요 🥰 / 2 재밌어했어요 😄 / 1 그냥 봤어요 🙂  (src/lib/reactions.ts)
  - 홈 피드(=커뮤니티 추천)에는 반응 2 이상만 노출.
    '그냥 봤어요'(1)는 개인 기록(마이페이지)과 책 단위 집계에 남는다.
    ⚠️ 반응 1을 DB에서 제외하거나 집계에서 빼지 말 것.
       "이 책은 12개월엔 반응이 낮다가 24개월부터 올라간다"는 분석이 월령 큐레이션의
       핵심이며, 반응 1이 있어야 성립한다.
  - ⚠️ 피드 노출 조건을 사용자에게 안내하지 말 것 (9장 ⓘ 문구 참조).

■ 세 가지 신호 축 — 서로 통합하지 말 것 (2026.07.31 정리)
  ① 기록 + 우리 아이 반응 : 모집단 = 읽어주고 기록까지 쓴 사람
       → 책 검증(월령별 반응). 1건당 1개 값이라 변별력이 없고, 자기신고 값이라 정렬엔 부적합.
         이미 '피드 노출 자격' 필터로 쓰고 있어 정렬에 또 쓰면 이중 사용이 된다.
  ② 나도 추천해요 : 모집단 = 읽었지만 기록은 안 쓴 사람 포함. 남이 눌러주는 N명 집계값.
       → 책 단위 저장(likes.book_id, 2026.08 전환). 홈 피드(책 카드) 정렬 + 추천 시기(group_names) 집계
  ③ 저장(북마크) : 모집단 = 아직 안 읽은 사람 → 개인 위시리스트. 랭킹 미사용
  ※ ②와 ③은 모집단이 배타적이다(이미 읽었으면 저장하지 않는다).
    합산하면 서로 다른 모집단을 섞게 되어 의미가 훼손된다.
  ※ 결론: 반응 = 필터 / 추천 = 정렬 / 저장 = 개인 리스트.

■ 홈 피드 랭킹 (시기 섹션별 독립 판정)
  - 시기 누적 기록 < 10건 → 최신순
  - 10건 이상 → 추천 수 → 반응 → 최신순

■ 월령 데이터는 전부 사용자 판단에서 나온다 (자동 부착 금지)
  - 기록 시: 작성자가 시기 칩을 직접 선택 (세그먼트로 '지금/예전' 구분)
  - 추천 시: 바텀시트로 추천 시기를 직접 질문
  - ⚠️ 어떤 경로에서도 "현재 시점의 아이 월령"을 데이터로 저장하지 말 것.
    입력 시점과 실제 읽은 시점이 다르면 체계적 상향 편향이 생기며 표본이 커져도 상쇄되지 않는다.

DB: posts.child_reaction smallint(1~3, default 2, CHECK). reread는 reaction===3일 때 true로 하위호환 유지.
    likes.group_names text[] (추천 시기 다중 선택).

차별점: 서점 리뷰(어른이 본 완성도)·독서기록앱(개인 권수)·전집 플랫폼과 달리
       "○○개월 아이의 실제 반응 + 집단이 검증한 추천 시기" 데이터 + 월령 큐레이션.
       ※ 경쟁 서비스(아기곰 책육아 등)는 전집 단위로 등록되어 개별 책의 월령 데이터가
         자동으로 쌓이지 않고, 사용자에게 사후 라벨링('몇 개월?')을 요청하는 구조다.
         또한 단권 추천이 '이미 소장한 책의 활용'이지 '미소장 책의 탐색'이 아니다.
         책육아정원은 단행본 탐색이며, 모든 월령 데이터가 입력 시점의 사용자 판단에서 나온다.

18.2 명칭 규칙 (확정, 코드 반영됨)
개인 행위 = '기록' / 발견·집계·SEO = '추천' 유지.
  - 하단 탭 '추천하기' → '기록하기'
  - 폼 헤더/버튼 '책 추천하기'·'추천 등록하기' → '책 기록하기'·'기록 남기기'
  - 폼 그룹 라벨 '추천 그룹' → '이 시기 아이가 읽으면 좋아요'
  - 한줄 메모 → '책육아 일기' (읽어주기 꿀팁·아이와 나눈 대화 등)
  - 마이페이지 '내 추천 목록' → '책육아 기록', '추천한 책' → '함께 읽은 책'
  - 상세 헤더 '추천 기록' → '책육아 기록'
  - 홈 피드 섹션·SEO 타이틀의 '추천'은 유지(집계 결과).
  - 반응 버튼 '좋아요' → '나도 추천해요' (아이콘: 하트 → 👍 엄지)
    * 라벨·동작 변경. DB 테이블명(likes)은 유지, group_names 컬럼 추가.
    * 의미: "나도 이 책을 이 시기 아이에게 추천한다". 글이 아니라 책·시기에 대한 판단.
    * 누른 후 표기 — 카드: '추천함' / 상세: '추천함 · 꽃잎 +2'
  - 화면에 보이는 말은 '시기', 코드·DB·스키마는 'group'을 쓴다.
    * 화면에 '그룹'이라는 단어를 노출하지 말 것.
      책육아정원은 실제 모임을 하는 브랜드라 사용자가 '참여 모임'으로 오독한다.
    * 씨앗→새싹→꽃잎→열매→나무는 자라나는 순서이므로 '시기'가 이름과 정합한다.
      '어른'도 성인 시기로 읽히므로 6개 전부 시기로 통일한다.
    * 코드는 group_names / post_groups / lib/groups.ts 그대로 유지. 마이그레이션 불필요.

18.3 예정 기능
A. 도서 검색 UX 개편: 기록 폼의 자동완성 드롭다운 → 전체화면 검색 오버레이.
   - 별도 라우트 대신 같은 페이지 오버레이 권장(폼 입력값 보존·뒤로가기 안전).
   - 표지 크게/정보 넉넉히, "아래로 스크롤했다 위로" 문제 해소. 우선순위 낮음.
B. 저장(북마크) 구현 — 추천과 별개 장치.
   - bookmarks를 book_id 단위로 전환 + 카운터 트리거.
   - 마이페이지 2분할: ① 아이와 지금까지 읽은 책(posts) / ② 나중에 아이와 읽으려고 저장한 책.
   - 저장 수는 5명 이상일 때만 표시. 명단 비공개. 랭킹 미사용.
C. 기록 폼 '우리 아이 반응' ⓘ — "솔직한 반응이 다른 양육자들에게 도움이 돼요"
   (피드 노출 조건 미표기)
D. '좋아요' → '나도 추천해요' — 👍 + 바텀시트(10.3) + group_names 저장.
E. 알라딘 외국도서 병렬 호출 (13.1 [B]) + 106165를 FOREIGN_CATEGORY_IDS로 재배치.
F. 기록 폼 세그먼트 버튼(지금 읽고 있어요 / 예전에 읽었어요) + 시기 자동 체크 (9.2).
G. 책 단위 통합 — 홈 피드 책 단위 묶기 + /book/[isbn] + 시기별 정렬 전환 로직.
   ⚠️ 셋을 한 작업으로 처리할 것. 카드 단위가 바뀌면 집계 기준도 바뀐다.

19. SEO 전략 (책 단위 페이지 + SSR + 동적 메타데이터)

19.1 핵심 원칙
사용자가 쌓은 기록 페이지가 검색엔진에 노출되는 구조다.
따라서 게시물 상세·책 페이지가 크롤러에 "내용이 담긴 HTML"로 읽혀야 한다.
도메인은 아직 구매하지 않았다(bookgardening.com이 유력 후보, 9단계에서 결정).
도메인 문자열은 SEO 검색어에 직접 영향을 주지 않으며, `돌아기 그림책 추천` 같은 한국어 검색어 노출은 페이지 title·콘텐츠가 결정한다. 따라서 도메인 확정을 기다리지 말고 19.2를 먼저 구현한다.

19.2 구조적 결정 (구현 시 반드시 준수 — 나중에 고치기 비쌈)

1. 상세·책 페이지는 Server Component로 SSR 한다.
   - 게시물 상세, 책 페이지의 본문 데이터는 서버에서 fetch 후 렌더링.
   - 본문 데이터를 useEffect + 클라이언트 Supabase 호출로 가져오지 말 것. (초기 HTML이 비어 크롤러가 내용을 못 봄)

2. 책 단위 페이지를 신설한다: /book/[isbn]
   - 기록 1건당 페이지(/posts/[id])와 별개로, 책 1권당 페이지를 둔다.
   - 같은 책에 기록이 누적될수록 한 URL에 콘텐츠와 검색 가중치가 집중된다. (10.2의 "같은 책 다수 기록 카드 누적" 설계와 일치)
   - slug는 ISBN13을 사용한다. (한글 제목 slug는 URL 인코딩 문제 → ISBN 권장)
   - 기존 /posts/[id]는 그대로 남겨둔다 (직접 링크·공유 대응).

3. generateMetadata로 페이지별 title/description을 동적 생성한다.
   - 책 페이지 title 패턴: {책제목} - {시기}({개월범위}) 그림책 추천 | 책육아정원
   - 예: 달님 안녕 - 씨앗(0~12개월) 그림책 추천 | 책육아정원
   - description: 기록 수 + 대표 기록 일부를 동적 삽입.

4. 책 페이지에는 '추천 시기 분포'를 상시 노출한다.
   - 이 페이지는 정보를 보러 온 사람을 위한 것이므로 분포를 숨길 이유가 없다.
     (바텀시트에서만 선택 완료 전 숨김. 10.3 참조)
   - generateMetadata의 {시기}는 이 분포의 최빈 시기를 사용한다.
     최빈 시기가 40% 미만이어도 타이틀은 단일값이어야 하므로 최빈값을 그대로 쓴다.
   - 표가 5개 미만이면 분포 막대 대신 텍스트로 대체하고, 타이틀의 시기는
     작성자가 고른 시기 중 가장 어린 것을 사용한다.
   - 분포 컴포넌트는 바텀시트와 공유한다 (src/components/book/PeriodDistribution.tsx).

19.3 배포 단계에 추가 (additive — 나중에 덧붙여도 됨)
- app/sitemap.ts, app/robots.ts (배포 직전)
- OG 태그 ✅ 구현됨(2026.08): layout.tsx 기본 OG + metadataBase(env NEXT_PUBLIC_SITE_URL), book·posts generateMetadata에 openGraph/twitter(표지 이미지). ⚠️ 배포 시 NEXT_PUBLIC_SITE_URL을 실제 도메인으로 설정해야 og:url이 정확해진다.
- JSON-LD 구조화 데이터 (Book 스키마, 출시 후 고도화)


20. 변경 이력
버전
날짜
내용
PRD v0.1
2026.05
최초 작성
CLAUDE.md 통합본
2026.06
PRD + 개발 확정사항 + VS Code 작업 내역 통합. 앱명 "책육아정원"으로 변경. 추천하기 경로 /recommend/create 확정.
CLAUDE.md v1.1
2026.07
SEO 전략 섹션(책 단위 페이지 /book/[isbn] + SSR + generateMetadata 동적 메타데이터) 추가.
CLAUDE.md v1.2
2026.07
1~3단계 완료. 실제 정규화 DB 스키마·저장 파이프라인(13.3) 문서화, RLS 정책(supabase/rls_policies.sql) 추가. 알라딘 검색 CID 기준+자동완성 드롭다운으로 변경(13.1). users kakao_id 버그 및 저장 테이블(recommendations→posts) 버그 수정.
CLAUDE.md v1.3
2026.07
4~6단계 완료(홈 피드·상세·마이페이지·좋아요·댓글). 제품 모델 확정(18장): 개인=기록/커뮤니티=추천, '우리 아이 반응' 3단계(child_reaction)로 재독 흡수, 명칭 rename. 예정 기능 A(검색 전체화면)·B(저장 강조+마이페이지 2분할)·C(반응 ⓘ 안내) 문서화. 나이 계산 일(day) 반영 수정. 첫 커밋 049e8e3.
CLAUDE.md v1.3.1
2026.07
진행 상황 최신화: 15(현재 상태 표·파일구조·미완료)·16(로드맵 4~6 완료, 6.5 예정) 갱신. 17에 '같은 책 통합 노출(/book/[isbn])' + 미결 '댓글 위치(가/나/다)' 추가.
CLAUDE.md v1.4
2026.07.31
경쟁 서비스 분석(아기곰 책육아·키즈북코드) 반영. 신호 축 3개 분리 명문화(18.1): 반응=필터 / 나도 추천해요=정렬 / 저장=개인 위시리스트, 상호 통합 금지. '좋아요'→'나도 추천해요'(👍) 전환, 탭 시 바텀시트로 추천 시기를 직접 질문(10.3) — 누른 시점의 아이 월령과 실제 읽은 시기가 달라 체계적 상향 편향이 발생하므로 월령 자동 부착 전면 폐기. likes.group_names(text[]) 추가. 시트 내 분포는 선택 완료 후에만 노출(앵커링 방지), 책 페이지는 상시 노출(19.2-4). 기록 폼은 세그먼트 버튼(지금 읽고 있어요/예전에 읽었어요) + 시기 칩 구조로 변경(9.2) — 설명 캡션 대신 컨트롤로 자동 선택을 노출, 모달·바텀시트 미사용. 홈 피드 정렬을 시기별 10건 기준 최신순↔추천순 전환으로 변경(6.2). 반응 ⓘ 문구에서 피드 노출 조건 제거(반응 인플레이션 방지). bookmarks를 post_id→book_id 전환, books.bookmark_count 트리거 추가, 저장 수는 5명 이상만 노출. 아이 나이 태그는 입력 보조 전용으로 격하하고 상세 ①영역 표시 제거(10.1). 알라딘: 106165가 외국도서 CID임을 확인해 CATEGORY_IDS에서 제거하고 FOREIGN_CATEGORY_IDS로 재배치, SearchTarget=Foreign 병렬 호출 추가(13.1). 화면 표기는 '시기', 코드·DB는 'group'으로 용어 분리(18.2). 앱 이름 '책육아정원' 확정. 책 단위 묶기·/book/[isbn]·정렬 로직을 6.7단계로 묶음.
CLAUDE.md v1.4.1
2026.08
시기 명칭 변경: 봄꽃→꽃잎, 열매(구 사과) — 성장 서사(씨앗→새싹→꽃잎→열매→나무) 정합. DB 마이그레이션 supabase/rename_groups_flower_fruit.sql(post_groups·likes CHECK 재정의+데이터 치환) 실행 완료. 코드 value(springflower/apple)·컬러 토큰은 유지, 화면 라벨만 변경. 시기를 단어로 쓸 때는 씨앗/새싹/꽃잎/열매/나무/어른으로 통일. 분포도(PeriodDistribution)에 시기명 아래 연령 기간 병기. 추천 후 표기는 시기 한글 대신 아이콘(추천함 · 🌸 +2), 홈 피드 카드는 글자 없이 엄지 아이콘 채움+색으로 추천 여부 표현. 상세 ③에서 추천·저장 버튼 한 줄 정렬. 개발용 분포 샘플 /dev/distribution (배포 전 삭제).
CLAUDE.md v1.5
2026.08
6.5·6.7단계 완료, 완료율 약 80%. 책 단위 통합: 홈 피드 책 1권=카드 1개(대표 반응 최빈·기록 N), /book/[isbn] SSR+generateMetadata(분포 최빈 시기), 시기별 정렬 전환(ranking.ts, 임계 10), 분포 집계 lib/distribution.ts 공용. 홈 카드 정보 우선순위 확정(아이반응>기록수·주제>추천·저장, 글밥량 미표시), 책 페이지 정보 배치(책 정보=객관(글밥량 대표값)/기록 카드=양육자 평가). 연령 표기 M/Y 축약(ageLabel), SEO는 한글(AGE_LABEL_FULL). 👍 의미 충돌 해소: 추천을 책 단위로 전환(migrate_likes_to_book.sql, likes.book_id PK), API /api/books/[id]/recommend, 버튼은 책 정보 영역으로(기록 카드 제거). 댓글 위치 (가) 기록별 확정. 샘플 데이터 시드(seed_sample.sql, 배포 전 cleanup 필수).
CLAUDE.md v1.6
2026.08
7단계 대부분 완료(완료율 ~85%). 첫 기록 게이트(11.4): 가입→온보딩→첫 기록→홈. 검색 완성: 두 섹션(우리 DB search_books 제목+작가 유사 검색 / 알라딘 기록 없는 책→첫 기록 유도), 정렬 정확일치→세일즈포인트. 시기 경계 조정(열매 31~48M, 나무 49M~). 그룹 전체보기 /group/[value](2열 그리드). 시기 배지 연령 오름차순 통일. 사진 실제 업로드(Storage post-images 버킷+RLS). 추천 시기 분포에 기록자(post_groups) 포함(유저별 합집합), 모수<5 멘트 개선. 카드 '기록 N'→아이콘. SNS 공유(ShareButton: Web Share+링크복사) + OG/트위터 태그(표지 미리보기).
