책육아정원 — CLAUDE.md (최종 통합본, 2026.06)
Claude Code가 매 작업 시작 시 이 파일을 먼저 읽고 프로젝트 맥락을 파악할 것. PRD 원문 + 개발 중 확정된 세부 결정사항 + 현재 진행 상태 모두 포함.


⚙️ Claude 행동 규칙 (필수)
답변마다 확신도 10점 만점으로 표기. 6점 이하면 이유 설명.
놓친 사항이 있으면 먼저 알려줄 것 (작업 시작 전 체크).
코드 제공 시 JSX 태그 쌍, import 누락 등 문법 오류를 반드시 사전 검토할 것.
각 단계 완료 후 다음 단계로 자동 진행하지 말고, 완료 보고 후 대기할 것.


1. 서비스 개요
앱명(임시): 책육아정원 형태: 그림책 추천 커뮤니티 모바일 웹앱 한 줄 소개: 부모가 아이와 함께 읽은 그림책을 월령·연령에 맞게 기록하는 "책육아 일기"이자, 개인 기록이 쌓여 커뮤니티 추천 플랫폼이 되는 서비스.

핵심 기능 우선순위 (중요):

아이와의 책 기록을 남기는 육아일기 기능 (가장 핵심)
알라딘 Open API를 활용한 책 정보 연동
개인들의 책육아 기록이 쌓여 형성되는 그림책 추천 커뮤니티

⚠️ 이 서비스는 "커뮤니티"가 1차 목적이 아니라 "개인 기록(일기)"이 1차 목적이고, 그 기록들이 모여 커뮤니티가 되는 구조. UI/UX 결정 시 이 우선순위를 기준으로 판단할 것.


2. 기술 스택 & 개발 환경
구분
기술
비고
프론트엔드
Next.js (App Router)
SSR로 OG 태그 / SEO 지원
백엔드 / DB
Supabase
무료 플랜 — DB 500MB, 스토리지 1GB
배포
Vercel
무료 플랜 — 초기 운영비 0원
스타일링
Tailwind CSS
모바일 우선 반응형 필수
책 정보
알라딘 Open API
제목·저자·출판사·링크·절판 여부
소셜 로그인
카카오 로그인
OAuth 2.0 (Supabase Auth 연동)


개발 환경: Windows, VS Code / Cursor, Node.js v24+ 프로젝트 경로: C:\Users\김가현\chaekugajungwon


3. 설계 원칙
제1원칙: 모바일 웹 반응형 UI — 절대 원칙 모든 화면은 모바일 환경을 최우선으로 설계한다. PC 웹에서도 사용 가능하되, 레이아웃과 인터랙션 기준은 항상 모바일이다. 이 원칙은 어떠한 이유로도 예외가 없다.


4. 사용자 및 권한
역할
권한 범위
일반 사용자
본인 게시물·댓글 작성/수정/삭제, 좋아요·북마크, 댓글 작성
운영자
일반 사용자 권한 + 타인 게시물·댓글 삭제, 주제 태그 관리, 특정 계정에 운영자 권한 부여


운영자 전용 관리 페이지는 별도로 만들지 않는다. 권한 부여는 Supabase 관리 콘솔 또는 운영자 계정 내 설정으로 처리한다.


5. 화면 구조 (네비게이션)
하단 탭 바 3개 + 상단 검색 아이콘으로 구성.

탭
아이콘
화면 설명
홈 피드
홈 아이콘
추천 그룹별 게시물 탐색 메인 화면
추천하기
플러스 아이콘
새 게시물 작성 폼 (/recommend/create)
마이페이지
사용자 아이콘
프로필, 아이 정보, 통계, 내 추천 목록
검색
돋보기 아이콘 (상단 고정)
책 제목 입력 → 관련 게시물 노출



6. 홈 피드 상세 스펙
6.1 전체 구조
상단 고정 헤더: 앱 로고 + 검색 아이콘
전체 주제 태그 필터 바: 운영자가 설정한 태그 가로 스크롤 칩
추천 그룹 섹션 × 6개 (씨앗/새싹/봄꽃/사과/나무/어른) 세로 배치
6.2 추천 그룹 섹션 구조 (각 섹션 동일 패턴)
섹션 헤더: 그룹 이름 배지 + 연령 설명 + 더보기 버튼
서브 태그 필터 바: 해당 그룹에 속한 운영자 태그 가로 스크롤 (밀리의서재 탭 방식 참고)
게시물 카드 가로 스크롤 목록: 기본 3개 노출 → 옆으로 스크롤 시 최대 12개
더보기 버튼 → 해당 그룹 전체 게시물 리스트 페이지로 이동
정렬 기준: 좋아요 많은 순 (인기순)
UI 레퍼런스: TripAdvisor 홈 피드 구조 참고
6.3 홈 피드 카드에 표시되는 정보
정보 항목
표시 방식
책 표지 이미지
알라딘 API 이미지 or 색상 플레이스홀더
책 제목
최대 2줄
추천 그룹
컬러 배지
주제 태그 (해시태그)
운영자 태그 최대 2개
글밥량
별 아이콘 1~5개
우리 아이 반응
이모지+짧은라벨(🥰 자꾸 꺼내봐요 / 😄 재밌어했어요) — 제목 아래 캡션. (18장 참조)
좋아요 수
하트 아이콘 + 카운트 (예정: '저장하기' 강조로 전환 — 18.3 참조)


카드 탭 시 게시물 상세 페이지로 이동.


7. 추천 그룹 기준 (연령 + 색상, 확정)
그룹
연령 범위
컬러(hex)
컬러명
씨앗
0~12개월
#A47551
흙빛 브라운
새싹
12~18개월
#A6D879
밝은 연두 (돌 전후 아기 상징, 밝고 산뜻한 톤)
봄꽃
19~30개월
#E2A0B5
분홍 블라썸
사과
31개월~4살
#D9614A
사과 코랄
나무
5살 이상
#4F6B43
짙은 숲그린
어른
어른도 즐길 수 있는 그림책
#6B3347
깊은 보르도 (와인톤)


게시물 작성 시 추천 그룹은 다중 선택 버튼으로 복수 선택 가능.

📌 기본 컬러(메인/포인트/배경/텍스트)는 아직 미확정. 임시: 모스그린 #6B7F5E(메인) / 살구코랄 #E08F5B(포인트) / 한지 아이보리 #FBF6EC(배경) / 잉크브라운 #3A332B(텍스트) 확정 전까지 재논의 가능. Tailwind 등록 시 "확정 아님" 주석 필수.


8. 태그 시스템
8.1 운영자 태그
운영자가 직접 생성 및 관리
홈 피드 상단 필터 바 및 각 추천 그룹의 서브 필터에 표시
태그 클릭 시 해당 태그가 붙은 게시물만 필터링 (노션 장르별 보드 방식)
태그 비활성화는 삭제 대신 is_active 필드로 처리 (히스토리 데이터 보존)
Supabase에 시드 데이터로 이미 등록 완료

운영자 태그 목록 (확정, 23개): 가족, 친구, 계절, 명절/기념일, 인성/감정/회복탄력성, 다양성, 공룡, 동물, 식물, 곤충, 음식, 생활습관, 색깔, 잠자리독서, 똥/방귀, 말놀이/수놀이, 몸/신체, 기관, 탈것, 캐릭터, 보드북, 조작북, 병풍/팝업책

📌 추천 그룹(연령)과 주제 태그(소재/형태)는 별개의 분류축. 한 게시물에 동시에 붙일 수 있음. 예: "사과" 그룹 + "공룡" 태그 + "보드북" 태그.
8.2 사용자 추가 태그
게시물 작성 시 자유롭게 추가 가능
피드와 게시물 상세에 해시태그 형태로 표시
필터링·분류에는 적용되지 않음 (표시 전용)


9. 게시물 입력 필드 스펙
필드
타입
설명
책 정보
알라딘 API 검색
제목, 저자, 출판사, 링크, 절판 여부 자동 입력
추천 그룹
다중 선택 버튼
씨앗/새싹/봄꽃/사과/나무/어른 중 복수 선택 가능
아이 나이 태그
프로필 연동 자동 계산
프로필에 등록된 아이 기준, 여러 명 탭 추가 가능
우리 아이 반응 (재독 여부 대체)
3단계 선택 버튼
자꾸 꺼내봐요(🥰,3) / 재밌어했어요(😄,2) / 그냥 봤어요(🙂,1). 옆에 ⓘ 아이콘으로 "자꾸 꺼내봐요·재밌어했어요는 홈 추천 피드에 노출" 안내. (18장 참조)
글밥량
5단계 선택
아래 기준표 참조
주제 태그
칩 선택 + 직접 입력
운영자 태그 선택 + 사용자 추가 태그 입력 가능
추천 이유
자유 텍스트
제한 없는 자유 메모란
사진
이미지 업로드
최대 3장 (용량 제한)

9.1 글밥량 5단계 기준 (정확히 이대로 구현)
단계
그림:글 비율
기준
★☆☆☆☆
그림 90% / 글 10%
페이지당 1~2단어
★★☆☆☆
그림 80% / 글 20%
한 페이지 1문장 내외
★★★☆☆
그림 70% / 글 30%
한 페이지 2~3문장
★★★★☆
그림 60% / 글 40%
페이지당 여러 문장
★★★★★
그림 50% 이하 / 글 50% 이상
글 많은 편



10. 게시물 상세 페이지
10.1 영역 구분 (3개 영역)
① 책 정보 영역

책 표지 이미지 / 책 제목 / 저자 / 출판사·출간일
아이 나이 (게시물 작성 시 태그된 아이 나이)
더보기 버튼 → 알라딘 해당 상품 링크로 이동

② 작성자 추천 카드 영역

작성자 닉네임 + 아이 띠/나이
추천 그룹 배지 / 주제 태그(운영자 태그 + 작성자 추가 태그)
글밥량(별점) / 우리 아이 반응(이모지+라벨) / 책육아 일기(자유 메모) / 사진(최대 3장)

③ 반응 영역

하트 아이콘 좋아요 버튼 (클릭 시 카운팅, 재클릭 시 취소)
댓글 창 (감상평 및 후기 작성)
10.2 같은 책 다수 추천 처리
동일 책에 여러 사용자가 추천 게시물을 작성한 경우, 책 정보 영역은 하나로 공유하고 그 아래에 추천 카드를 순서대로 누적 표시. (구체적 UI는 실제 구현 시 최종 확정)


11. 프로필 스펙
11.1 기본 정보
닉네임: 카카오 이름 기본값, 사용자가 직접 수정 가능
로그인 수단: 카카오 로그인 (OAuth 2.0, Supabase Auth 연동)
11.2 아이 정보
아이 생년월일 다수 등록 가능
십이지신 아이콘으로 시각적 구분
나이 자동 표기 로직 (정확히 이대로 구현):

개월 수
표기
36개월 미만
OO개월
36~47개월
만 3살
48~59개월
만 4살
60~71개월
만 5살
72개월 이상
초등학생 이상

11.3 마이페이지 구성 (2026.07 개편 — 18.3 참조)
프로필 (닉네임 수정, 아이 정보 관리)
독서 통계: 함께 읽은 책(중복 제외) + 책육아 기록 수
책육아 기록을 두 목록으로 분리:
  ① 아이와 지금까지 읽은 책 — 내가 기록(posts)한 것
  ② 나중에 아이와 읽으려고 저장한 책 — 저장(bookmarks)한 것
현재 구현: '책육아 기록'(=①) + 통계까지 완료. ②(저장 목록)는 예정.


12. 공통 기능
기능
설명
카카오 로그인
OAuth 2.0 기반 소셜 로그인
SNS 공유
게시물 외부 공유 (카카오·링크 복사 등)
좋아요
하트 아이콘. 클릭 시 +1, 재클릭 시 -1. 실시간 카운팅. (예정: 저장하기 대비 비중 축소 — 18.3)
북마크/저장 ★강조
'나중에 아이와 읽기'용 저장. 마이페이지 ②목록의 소스. 좋아요보다 유용한 신호로 전면 강조 예정. (18.3)
댓글
감상평·후기 작성. 작성자는 본인 댓글만 삭제 가능
검색
책 제목 입력 → 해당 책이 포함된 게시물 노출
신고 기능
⏸ MVP 이후 추가 예정



13. 외부 API 연동 스펙
13.1 알라딘 Open API (2026.07 갱신)
사용 API: 상품 검색 API (ItemSearch.aspx)만 사용
요청 파라미터 확정값:
QueryType=Title (제목 검색)
SearchTarget=Book
Cover=Mid / Output=JS (JSON) / Version=20131101
카테고리(CID) 기준 검색으로 변경: 그림책 위주로 좁히기 위해 지정한 CID만 호출.
  - CATEGORY_IDS = [1108, 13789, 2030, 106165]
    (1108=유아>4~7세>그림책, 13789=유아>0~3세>그림책, 2030·106165=추가 그림책 분류)
  - ItemSearch는 CategoryId를 하나만 받으므로 CID별로 병렬 호출(Promise.all) 후
    isbn13(없으면 link) 기준으로 중복 제거해 합침. CID당 MaxResults=30.
  - CID 추가는 CATEGORY_IDS 배열에 숫자만 추가하면 됨.
  - ⚠️ CID가 국내도서 트리 기준이라 현재 외국도서(번역 그림책)는 제외됨. 외서 CID는 추후 추가.
mallType 필터: 응답에서 BOOK(국내도서)·FOREIGN(외국도서)만 사용, EBOOK/DVD/음반/중고 제외.
절판 여부: stockstatus !== '' && stockstatus != null → isOutOfPrint: true
검색 UX: 추천하기 화면은 입력 300ms 디바운스 후 자동 검색 → 검색창 아래 자동완성 드롭다운(표지+제목)으로 선택.
  (예정 개편: 드롭다운 → 전체화면 검색 오버레이. 폼 상태 보존 위해 별도 라우트가 아닌 같은 페이지 오버레이 방식 권장. 18.3 참조)
구현 위치: src/app/api/aladin/search/route.ts ✅ 완료
환경변수: .env.local의 ALADIN_TTB_KEY
13.2 카카오 로그인 (Supabase Auth 연동)
scope: profile_nickname profile_image만 명시 (account_email 제외)
PKCE 쿠키는 NextResponse.redirect()에 수동으로 배열 수집 후 적용
Supabase URL Configuration에 등록된 Redirect URLs:
http://localhost:3000/auth/callback
http://localhost:3000/**
Server Component의 setAll 쿠키 핸들러는 반드시 try/catch로 감쌀 것

13.3 Supabase DB 스키마 & 저장 파이프라인 (2026.07 확정)
⚠️ 게시물은 단일 테이블이 아니라 정규화 구조다. (초기 코드가 존재하지 않는 'recommendations' 테이블에 저장하려다 실패 → 아래 구조로 수정 완료)
주요 테이블/컬럼:
  - users: id(uuid, auth.users와 동일), kakao_id(varchar NOT NULL), nickname, is_operator, created_at
  - children: id, user_id, label(기본 '아이'), birth_date, created_at
  - books: id, aladin_item_id(varchar NOT NULL, 실제로는 ISBN13 저장), title, author, publisher,
           published_date(date), cover_image_url, aladin_url, is_out_of_print, fetched_at
  - posts: id, user_id, book_id(→books), reread(bool), text_density(smallint 1~5), content(메모), created_at, updated_at
  - post_groups: post_id, group_name  ← CHECK 제약: 한글 라벨('씨앗'/'새싹'/'봄꽃'/'사과'/'나무'/'어른')만 허용
  - post_tags: post_id, tag_id(→operator_tags, nullable), custom_tag(nullable), is_operator_tag
               ← CHECK 제약(chk_tag_type): 운영자태그면 tag_id 있고 custom_tag NULL / 커스텀이면 반대
  - post_images: post_id, image_url, sort_order
  - post_children: post_id, child_id
  - operator_tags: id, name, tag_category, is_active, created_by, created_at
  - likes: user_id, post_id, created_at / bookmarks: user_id, post_id, created_at
  - comments: id, post_id, user_id, content, created_at
추천 저장 순서(src/app/api/recommend/route.ts):
  books(aladin_item_id=ISBN13로 있으면 재사용/없으면 insert) → users self-heal(upsert) → posts
  → post_groups(그룹 value를 한글 label로 변환) → post_tags(운영자태그는 id 연결). 중간 실패 시 posts 롤백.
그룹 저장 방식: post_groups에는 한글 라벨 저장. 폼의 그룹 value('seed' 등)는 lib/groups.ts 매핑으로 변환.
사진/아이 태그: post_images·post_children는 실제 Storage 업로드/아이 선택 UI 구현 시 연결 예정(현재 미저장).
RLS 정책: supabase/rls_policies.sql 에 정의. "누구나 읽기 / 본인 것만 쓰기(아이·북마크는 본인만 읽기),
          operator_tags 쓰기는 운영자만". 스키마 변경 시 이 파일도 갱신하고 SQL Editor에서 재실행.
users 행 생성: 카카오 콜백에서 kakao_id 포함해 insert. 누락 대비해 recommend/profile 라우트에서 upsert self-heal.


14. 디자인 시스템
14.1 디자인 무드 (중요)
키워드: 따뜻하고 포근하며 맑고 깔끔하고 자연스러운 느낌
톤앤매너: 그림책 종이 같은 감성. 육아일기처럼 부드럽고 기록하는 느낌.
금지 톤: 유아용 키즈 앱처럼 원색 과다·과도한 활기참·장난감 느낌 NO
배경: 흰색 아닌 따뜻한 아이보리(한지 느낌) 계열
14.2 UI 레퍼런스
홈 피드 전체 구조: TripAdvisor 홈 피드 참고
그룹별 서브 태그 필터: 밀리의서재 장르 탭 방식 참고 (가로 스크롤 칩)
14.3 폰트 방향 (미확정)
제목/서비스명: 손글씨 느낌의 한글 세리프 (예: 고운바탕)
본문/UI 텍스트: Pretendard 추천
확정 전까지 시스템 기본 폰트로 임시 처리 가능
14.4 확정된 컬러
추천 그룹 배지 6색 — 확정 (7번 섹션 참조)
14.5 미확정 컬러
기본 베이스 컬러(메인/포인트/배경/텍스트) — 재논의 필요. Tailwind 등록 시 "확정 아님" 주석 필수.
14.6 Tailwind 적용 방식
v3이면 tailwind.config.ts의 theme.extend.colors, v4이면 globals.css의 @theme 블록. 작업 전 Tailwind 버전 먼저 확인할 것.


15. 현재 프로젝트 상태 (2026.07 기준, 약 65%)  ← 마지막 커밋 049e8e3
완료된 작업
작업
파일
상태
Next.js 세팅 + 디자인 토큰(@theme, 그룹 배지 6색)
globals.css
✅
카카오 OAuth 로그인 + 콜백 + 로그아웃 + users self-heal
src/app/api/auth/*, src/app/auth/callback
✅
온보딩(닉네임·아이정보) + 프로필 저장 API
src/app/onboarding, src/app/api/auth/profile
✅
알라딘 검색 API(CID 기준 + mallType 필터)
src/app/api/aladin/search/route.ts
✅
기록하기 폼(검색 자동완성 드롭다운, 우리 아이 반응 3단계)
src/app/recommend/create/page.tsx
✅ 저장 검증 완료
기록 저장 API(books/posts/post_groups/post_tags 정규화)
src/app/api/recommend/route.ts
✅ 저장 검증 완료
홈 피드(SSR, 월령 6그룹 섹션, 태그 필터, 인기순 카드, 반응≥2만 노출)
src/app/page.tsx, src/lib/feed.ts, src/components/feed/*
✅
게시물 상세(SSR, 3영역, generateMetadata)
src/app/posts/[id]/page.tsx
✅
좋아요 토글 + 댓글 작성/삭제
src/app/api/posts/[id]/*, src/components/post/PostReactions.tsx
✅
마이페이지(프로필 편집, 독서 통계, 책육아 기록 목록)
src/app/mypage, src/components/mypage/ProfileEditor.tsx
✅
공통 레이아웃(하단 탭바, 상단 헤더)
src/components/BottomTabBar.tsx, FeedHeader.tsx
✅
아이 나이 자동 계산·갱신(일 단위 반영)
src/lib/age.ts, src/components/AgeLabel.tsx
✅
RLS 정책 + child_reaction 마이그레이션 SQL
supabase/rls_policies.sql, supabase/add_child_reaction.sql
✅ (Supabase 적용 완료)

미완료 (앞으로 만들 것)
예정 기능 A/B/C (18.3): 검색 전체화면 / 저장하기 강조 + 마이페이지 2분할 / 반응 ⓘ 안내  ← 다음 작업
책 단위 페이지 /book/[isbn] + 홈 피드 책 단위 묶기 (같은 책 통합 노출, SEO 핵심)
검색 화면(/search 현재 스텁) 고도화 및 필터
그룹 전체보기(/group/[value] 현재 스텁), 사진 실제 업로드(Storage, 현재 blob 미저장)
북마크(저장)·SNS 공유(카카오/링크)
운영자 태그 관리 / 권한·관리 기능
배포(Vercel) 및 도메인 설정


16. 전체 개발 공정 로드맵
단계
내용
상태
1단계
디자인 토큰 등록 (Tailwind 컬러 등록)
✅ 완료 (globals.css @theme, 그룹 배지 6색)
2단계
인증 마무리 (로그아웃 검증, users 테이블 upsert, 닉네임/아이정보 등록)
✅ 완료 (kakao_id 채움, onboarding, RLS, users self-heal)
3단계
추천하기 화면 검증 (Supabase 저장 검증, 검색 UX 최종 확인)
✅ 완료 (정규화 스키마 저장 검증, CID 검색 + 자동완성 드롭다운)
4단계
홈 피드 화면
✅ 완료 (SSR, 월령 6그룹, 태그 필터, 인기순, 반응≥2만 노출)
5단계
게시물 상세 페이지
✅ 완료 (3영역, generateMetadata, 좋아요·댓글)
6단계
마이페이지
✅ 완료 (프로필 편집, 통계, 책육아 기록)
6.5단계
예정 기능 A/B/C (검색 전체화면 / 저장 강조+마이페이지 2분할 / 반응 ⓘ) → 이후 /book/[isbn]
🔄 진행 예정 (다음 작업)
7단계
공통 기능: 좋아요✅ 댓글✅ / 북마크(저장)·검색·SNS 공유 ⏳
🔄 일부 완료
8단계
운영자 기능 (태그 관리, 타인 게시물 삭제)
⏳
9단계
배포 (GitHub → Vercel, 환경변수 이전, 도메인 설정)
⏳
10단계
배포 후 모바일 테스트 및 버그 수정
⏳



17. 미결 / 보류 사항
항목
상태
비고
앱 이름
미결정
"책육아정원" 임시 사용 중
기본 베이스 컬러
재논의 중
그룹 배지 컬러만 확정
타이포그래피
미확정
방향만 결정 (세리프+산세리프 페어링)
같은 책 통합 노출 (/book/[isbn])
설계 확정·미구현
현재 홈 피드는 "기록 1건=카드 1개"라 같은 책이 중복 노출됨. 예정: 책 1권=카드 1개로 묶고(집계: 추천 N명·합산 좋아요·대표 반응), 클릭 시 /book/[isbn]에서 상단 책 정보 + 여러 기록 카드 누적. 글밥량은 대표값(최빈/평균).
└ 미결정: /book/[isbn] 댓글 위치
book 페이지 착수 시 확정
(가) 기록별 댓글(현행, comments.post_id 그대로) / (나) 책 단위 스레드 / (다) 둘 다. 현재 추천=(가) 유지. ※ 댓글=기록 없이도 짧은 공감·반응 남기는 장치(좋아요와 함께 상세 ③반응 영역).
신고 기능
보류
MVP 이후 추가
검색 범위
미정
책 제목 기준 확정, 작성자/태그 검색은 추후 논의



18. 제품 모델 · 명칭 규칙 · 예정 기능 (2026.07 확정)

18.1 핵심 제품 모델 — "개인은 기록, 커뮤니티(집계)가 추천" (확정, 코드 반영됨)
트립어드바이저식 추천/비추천 이분 마크는 채택하지 않는다.
사용자는 읽은 책을 '기록'하고, 그 기록이 집계되어 커뮤니티 '추천'이 된다.
명시적 추천/비추천 대신 "우리 아이 반응" 3단계로 신호를 받는다 (재독 여부를 흡수):
  - 3 자꾸 꺼내봐요 🥰 / 2 재밌어했어요 😄 / 1 그냥 봤어요 🙂  (src/lib/reactions.ts)
  - 홈 피드(=커뮤니티 추천)에는 반응 2 이상(자꾸 꺼내봐요·재밌어했어요)만 노출.
    '그냥 봤어요'(1)는 개인 기록(마이페이지)에만 남고 피드엔 안 뜸.
  - 랭킹: 좋아요 우선, 동점이면 반응 좋은 순.
DB: posts.child_reaction smallint(1~3, default 2, CHECK). reread는 reaction===3일 때 true로 하위호환 유지.
    마이그레이션 파일 supabase/add_child_reaction.sql.
차별점: 서점 리뷰(어른이 본 완성도)·독서기록앱(개인 권수)과 달리 "○○개월 아이의 실제 반응" 집단 데이터 + 월령 큐레이션.

18.2 명칭 규칙 (확정, 코드 반영됨)
개인 행위 = '기록' / 발견·집계·SEO = '추천' 유지.
  - 하단 탭 '추천하기' → '기록하기'
  - 폼 헤더/버튼 '책 추천하기'·'추천 등록하기' → '책 기록하기'·'기록 남기기'
  - 폼 그룹 라벨 '추천 그룹' → '이 시기 아이가 읽으면 좋아요'
  - 한줄 메모 → '책육아 일기' (읽어주기 꿀팁·아이와 나눈 대화 등)
  - 마이페이지 '내 추천 목록' → '책육아 기록', '추천한 책' → '함께 읽은 책'
  - 상세 헤더 '추천 기록' → '책육아 기록'
  - 홈 피드 섹션·SEO 타이틀의 '추천'은 유지(집계 결과).

18.3 예정 기능 (아직 미구현 — 다음에 진행)
A. 도서 검색 UX 개편: 기록 폼의 자동완성 드롭다운 → 전체화면 검색 오버레이.
   - 모바일웹에서 표준 패턴(가능). 별도 라우트 대신 같은 페이지 오버레이 권장(폼 입력값 보존·뒤로가기 안전).
   - 표지 크게/정보 넉넉히, "아래로 스크롤했다 위로" 문제 해소.
B. '저장하기'(북마크) 전면 강조 — 좋아요보다 유용한 신호로 판단.
   - 마이페이지 책육아 기록을 두 목록으로 분리:
     ① 아이와 지금까지 읽은 책(내 posts) / ② 나중에 아이와 읽으려고 저장한 책(bookmarks).
   - 상세·카드에서 저장 버튼을 좋아요보다 강조.
C. 기록 폼 '우리 아이 반응' 3단계 옆에 ⓘ 아이콘 — "자꾸 꺼내봐요·재밌어했어요는 홈 추천 피드에 노출됩니다" 안내.

19. SEO 전략 (책 단위 페이지 + SSR + 동적 메타데이터)

19.1 핵심 원칙
사용자가 쌓은 기록 페이지가 검색엔진에 노출되는 구조다.
따라서 게시물 상세·책 페이지가 크롤러에 "내용이 담긴 HTML"로 읽혀야 한다.
도메인(bookgardening.com)은 SEO 검색어에 직접 영향을 주지 않으며, `돌아기 그림책 추천` 같은 한국어 검색어 노출은 페이지 title·콘텐츠가 결정한다.

19.2 구조적 결정 (구현 시 반드시 준수 — 나중에 고치기 비쌈)

1. 상세·책 페이지는 Server Component로 SSR 한다.
   - 게시물 상세, 책 페이지의 본문 데이터는 서버에서 fetch 후 렌더링.
   - 본문 데이터를 useEffect + 클라이언트 Supabase 호출로 가져오지 말 것. (초기 HTML이 비어 크롤러가 내용을 못 봄)

2. 책 단위 페이지를 신설한다: /book/[isbn]
   - 추천 1건당 페이지(/recommend/[id])와 별개로, 책 1권당 페이지를 둔다.
   - 같은 책에 추천이 누적될수록 한 URL에 콘텐츠와 검색 가중치가 집중된다. (PRD의 "같은 책 다수 추천 카드 누적" 설계와 일치)
   - slug는 ISBN13을 사용한다. (한글 제목 slug는 URL 인코딩 문제 → ISBN 권장)

3. generateMetadata로 페이지별 title/description을 동적 생성한다.
   - 책 페이지 title 패턴: {책제목} - {연령그룹}({개월범위}) 그림책 추천 | 책육아정원
   - 예: 달님 안녕 - 씨앗(0~12개월) 그림책 추천 | 책육아정원
   - description: 추천 수 + 대표 추천 이유 일부를 동적 삽입.

19.3 배포 단계에 추가 (additive — 나중에 덧붙여도 됨)
- app/sitemap.ts, app/robots.ts (배포 직전)
- OG 태그 (SNS 공유 기능 구현 시 함께)
- JSON-LD 구조화 데이터 (Book 스키마, 출시 후 고도화)


20. 변경 이력
버전
날짜
내용
PRD v0.1
2026.05
최초 작성
CLAUDE.md 통합본
2026.06
PRD + 개발 확정사항 + VS Code 작업 내역 통합. 앱명 "책육아정원"으로 변경. 추천하기 경로 /recommend/create 확정.
CLAUDE.md v1.1
2026.07
SEO 전략 섹션(책 단위 페이지 /book/[isbn] + SSR + generateMetadata 동적 메타데이터) 추가.
CLAUDE.md v1.2
2026.07
1~3단계 완료. 실제 정규화 DB 스키마·저장 파이프라인(13.3) 문서화, RLS 정책(supabase/rls_policies.sql) 추가. 알라딘 검색 CID 기준+자동완성 드롭다운으로 변경(13.1). users kakao_id 버그 및 저장 테이블(recommendations→posts) 버그 수정.
CLAUDE.md v1.3
2026.07
4~6단계 완료(홈 피드·상세·마이페이지·좋아요·댓글). 제품 모델 확정(18장): 개인=기록/커뮤니티=추천, '우리 아이 반응' 3단계(child_reaction)로 재독 흡수, 명칭 rename. 예정 기능 A(검색 전체화면)·B(저장 강조+마이페이지 2분할)·C(반응 ⓘ 안내) 문서화. 나이 계산 일(day) 반영 수정. 첫 커밋 049e8e3.
CLAUDE.md v1.3.1
2026.07
진행 상황 최신화: 15(현재 상태 표·파일구조·미완료)·16(로드맵 4~6 완료, 6.5 예정) 갱신. 17에 '같은 책 통합 노출(/book/[isbn])' + 미결 '댓글 위치(가/나/다)' 추가.


