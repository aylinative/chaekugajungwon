# 작업지시서 — 알라딘 Open API → 카카오 Daum 책 검색 API 전환 (최종본)

작성 2026-08-22 · 개정 2026-08-24 · 대상: 책육아정원 · 확신도: 8/10
사유: 알라딘 Open API 서비스 종료 예정. 도서 검색 백엔드를 카카오 Daum 책 검색 API로 교체.

> ※ 이 문서는 검증 스파이크(카카오 실측 + 재정렬 실측)를 거쳐 확정된 최종본이다.
> 검토 과정에서 국립중앙도서관(부가기호) 대안도 실측 검토했으나, 개인 접근 문턱·영어원서/판매상태 손실 때문에
> **A안(카카오 단독 + 재정렬)**으로 결정. 완전 카탈로그(국중도 대량수집)는 **B안으로 후속**(CLAUDE.md Deferred TODO).

---

## 0. 결정 요약

- **주 소스: 카카오 Daum 책 검색 API 단독.** (알라딘 대체, 국중도 미사용)
- **어댑터 방식:** 라우트가 프론트에 주던 기존 `items[]` 형태를 그대로 유지 → **프론트 2곳·저장 로직 무변경.**
- **재정렬 추가(Path A 핵심):** 카카오 결과에 `제목 중복정리(개정판 대표) + 그림책 출판사 부스트 + 문제집/세트 페널티`를 얹어
  일반 단어 검색의 카테고리 노이즈를 제거. **추가 API 호출 0건**(라우트 내부 연산).
- **ISBN/바코드 분기 신규:** ISBN 문자열이면 카카오 `target=isbn`. (기존 코드엔 없던 분기 — 신규 추가)
- **커버리지 보장(A안):** 라이브검색에 안 뜨는 책도 기록 가능하도록 백스톱(ISBN 조회 + 수동입력). 수동입력은 후속.

### 스파이크로 검증된 사실 (2026.08.22 실측, `.env.local`의 KAKAO_CLIENT_ID 사용)
| 항목 | 결과 |
|---|---|
| 응답 속도 | 평균 76ms(콜드 제외 34~53ms) — 자동완성 적합 |
| 표지 채움률 | 40/40 (100%) |
| 영어 원서 | Brown Bear·Very Hungry Caterpillar 원서 정상 노출 (알라딘 Foreign 불필요) |
| 판매상태 | `status` 정상판매/주문판매/절판 등 구분 |
| 개정판 정렬 | 구름빵 2019 개정판이 2007 구판보다 상위 |
| 재정렬 효과 | "동물" 1위 동물농장(성인소설)→그림책으로 교체, "사과" 문제집·성인서 상위 제거 |

---

## 1. 현황 분석 (전환 근거)

### 1-A. 현재 알라딘 라우트가 프론트에 주는 형태
```jsonc
// GET /api/books/search?query=... → 200  (구 /api/aladin/search에서 이동)
{ "items": [
  { "title", "author", "publisher", "pubDate", "cover", "link", "isbn13", "isOutOfPrint" }
]}
```
호출처 2곳: `src/app/recommend/create/page.tsx`, `src/components/search/SearchClient.tsx`

### 1-B. 프론트 필드 사용
| 파일 | 사용 필드 |
|---|---|
| recommend/create (`BookItem`) | 8개 전부 |
| SearchClient (`AladinItem`) | title, author, cover, isbn13 |

### 1-C. books 저장 로직 (`api/recommend/route.ts`)
| books 컬럼 | 소스 | 비고 |
|---|---|---|
| book_key (NOT NULL, 구 aladin_item_id) | isbn13 ‖ link ‖ title | 식별자 |
| title / author / publisher | 동명 | |
| published_date (DATE) | pubDate | `/^\d{4}-\d{2}-\d{2}$/` 검증 → 불일치 시 null |
| cover_image_url | cover | |
| source_url (구 aladin_url) | link | 외부 책 상세 링크 |
| is_out_of_print | isOutOfPrint | |

> DB 컬럼명은 카카오 전환에 맞춰 리네임: `aladin_item_id`→`book_key`, `aladin_url`→`source_url` (배포 전이라 저비용. supabase/rename_aladin_columns.sql). 값 의미 동일.

---

## 2. 카카오 API 매핑

- 엔드포인트: `GET https://dapi.kakao.com/v3/search/book`
- 인증: 헤더 `Authorization: KakaoAK {KAKAO_CLIENT_ID}` (REST 키. `.env.local`에 이미 존재)
- 파라미터: `query`(필수), `sort`(accuracy|latest), `size`(≤50), `page`, `target`(title|isbn|publisher|person)
- 응답: `documents[]` + `meta{total_count,...}` · 일일 30,000회

### 필드 매핑 (카카오 document → 내부 items)
| 내부 | 카카오 | 변환 · 주의 |
|---|---|---|
| title | `title` | 그대로 |
| author | `authors[]` **배열** | `authors.join(', ')` 필수 |
| publisher | `publisher` | 그대로 |
| pubDate | `datetime` (ISO8601) | `.slice(0,10)` → `YYYY-MM-DD` (DATE 검증 통과) |
| cover | `thumbnail` | 그대로. **coversum→cover500 치환 삭제**(알라딘 전용) |
| link | `url` | 다음 책 상세 URL |
| isbn13 | `isbn` (`"ISBN10 ISBN13"`) | 공백 split 후 13자리 우선 |
| isOutOfPrint | `status` | `/절판\|품절/.test(status)` (알라딘 `!==''` 로직 금지) |

---

## 3. 작업 단계

### STEP 1 — 환경변수
- 카카오 REST 키(`KAKAO_CLIENT_ID`)는 `.env.local`에 **이미 존재** → 로컬 추가 작업 없음.
- **배포 시 Vercel 환경변수에 `KAKAO_CLIENT_ID` 등록** 필요(9단계 체크리스트).
- 전환·검증 완료 후 `ALADIN_TTB_KEY` 제거.

### STEP 2 — 재정렬 로직 lib 신설: `src/lib/pictureBookRanking.ts`
```ts
// 그림책 검색 결과 재정렬 — 카카오 결과의 카테고리 노이즈를 라우트 내부에서 정리.
// (부가기호가 없는 상업 DB 한계를 '출판사 부스트 + 제목 페널티 + 개정판 중복정리'로 보완)

export interface BookItem {
  title: string; author: string; publisher: string; pubDate: string
  cover: string; link: string; isbn13: string; isOutOfPrint: boolean
}

// ① 시드: 그림책/유아 강세 출판사(부분일치). 콜드스타트용. 
//    ※ 후속: books(서로 다른 기록 ≥2건)에서 자동 도출로 전환하면 이 시드는 축소/제거 가능. (CLAUDE.md Deferred TODO)
export const PICTURE_PUBLISHERS = [
  '한림출판사','보림','시공주니어','비룡소','사계절','웅진주니어','웅진씽크빅','국민서관',
  '길벗어린이','책읽는곰','창비','미디어창비','한솔수북','더큰','제이와이북스','JYBooks',
  '어스본코리아','다산어린이','블루래빗','스토리보울','문학동네어린이','노란상상','노란돼지',
  '천개의바람','북극곰','봄봄','키즈엠','그레이트북스','애플비','청어람아이','아이세움',
  '한울림어린이','주니어김영사','상상의집','소원나무','위즈덤하우스','계림','예림당','을파소',
  '삼성출판사','풀빛','재미마주','키위북스','나는별','토토북','현북스','스콜라','뜨인돌어린이',
]
// ③ 구조적 페널티: 명백한 비그림책(문제집/세트/학년). 거의 불변.
const NON_PICTURE_TITLE = /문제집|자습서|기출|모의고사|워크북|전과목|세트|전집|\d\s*학년|\d\s*-\s*\d/

const norm = (s: string) => (s ?? '').replace(/\s+/g, '').toLowerCase()
export const isPicturePublisher = (p = '') => PICTURE_PUBLISHERS.some((n) => p.includes(n))

function score(it: BookItem, nq: string): number {
  const t = norm(it.title)
  let s = 0
  if (t === nq) s += 1000
  else if (t.startsWith(nq)) s += 500
  if (isPicturePublisher(it.publisher)) s += 200
  if (NON_PICTURE_TITLE.test(it.title ?? '')) s -= 150
  return s
}

// 제목 중복정리: 같은 정규화 제목은 출간일 최신(=개정판) 1개만 대표
function dedupNewest(items: BookItem[]): BookItem[] {
  const byTitle = new Map<string, BookItem>()
  for (const it of items) {
    const k = norm(it.title)
    const prev = byTitle.get(k)
    if (!prev || (it.pubDate || '') > (prev.pubDate || '')) byTitle.set(k, it)
  }
  return [...byTitle.values()]
}

export function rerankBooks(items: BookItem[], query: string): BookItem[] {
  const nq = norm(query)
  return dedupNewest(items)
    .map((it) => ({ it, s: score(it, nq) }))
    .sort((a, b) => b.s - a.s || (b.it.pubDate || '').localeCompare(a.it.pubDate || ''))
    .map((x) => x.it)
}
```
> ⚠️ `dedupNewest`는 **정확히 같은 제목만** 병합한다(부제 다른 시리즈는 유지). 지나친 병합 방지.

### STEP 3 — 카카오 라우트 신설 `src/app/api/books/search/route.ts` (구 `/api/aladin/search` 이동·삭제)
(라우트 경로·응답 형태 불변 → 프론트 무변경)
```ts
import { NextRequest, NextResponse } from 'next/server';
import { rerankBooks, type BookItem } from '@/lib/pictureBookRanking';

interface KakaoBook {
  title: string; contents: string; url: string; isbn: string; datetime: string;
  authors: string[]; publisher: string; translators: string[];
  price: number; sale_price: number; thumbnail: string; status: string;
}
interface KakaoResponse { documents?: KakaoBook[]; meta?: { total_count: number } }

const KAKAO_ENDPOINT = 'https://dapi.kakao.com/v3/search/book';
const PAGE_SIZE = 30; // 카테고리 필터가 없어 넉넉히 받아 재정렬로 상위 노출

function pickIsbn13(isbn: string): string {
  const tokens = (isbn ?? '').split(/\s+/).filter(Boolean);
  return tokens.find((t) => t.replace(/[^0-9Xx]/g, '').length === 13) ?? tokens[0] ?? '';
}
function looksLikeIsbn(q: string): boolean {
  const s = q.replace(/[\s-]/g, '');
  return /^\d{9}[\dXx]$/.test(s) || /^\d{13}$/.test(s);
}
async function searchKakao(query: string, key: string, target?: 'isbn'): Promise<KakaoBook[]> {
  const url = new URL(KAKAO_ENDPOINT);
  url.searchParams.set('query', query);
  url.searchParams.set('size', String(PAGE_SIZE));
  url.searchParams.set('sort', 'accuracy');
  if (target) url.searchParams.set('target', target);
  const res = await fetch(url.toString(), { headers: { Authorization: `KakaoAK ${key}` } });
  if (!res.ok) throw new Error(`카카오 책 검색 API 오류: ${res.status}`);
  const data: KakaoResponse = await res.json();
  return data.documents ?? [];
}

export async function GET(request: NextRequest) {
  const raw = request.nextUrl.searchParams.get('query') ?? '';
  const query = raw.replace(/\s+/g, ' ').trim();
  if (!query) return NextResponse.json({ error: '검색어(query)가 필요합니다.' }, { status: 400 });

  const key = process.env.KAKAO_CLIENT_ID; // 카카오 REST 키
  if (!key) return NextResponse.json({ error: '카카오 REST 키가 설정되지 않았습니다.' }, { status: 500 });

  try {
    const isIsbn = looksLikeIsbn(query);
    const documents = isIsbn
      ? await searchKakao(query.replace(/[\s-]/g, ''), key, 'isbn')
      : await searchKakao(query, key);

    const mapped: BookItem[] = documents.map((d) => ({
      title: d.title,
      author: (d.authors ?? []).join(', '),
      publisher: d.publisher,
      pubDate: (d.datetime ?? '').slice(0, 10),
      cover: d.thumbnail ?? '',
      link: d.url,
      isbn13: pickIsbn13(d.isbn),
      isOutOfPrint: /절판|품절/.test(d.status ?? ''),
    }));

    // ISBN 정확검색은 재정렬 불필요(단건 정확). 제목/키워드 검색만 재정렬.
    const items = isIsbn ? mapped : rerankBooks(mapped, query);
    return NextResponse.json({ items });
  } catch (error) {
    console.error('카카오 책 검색 API 호출 실패:', error);
    return NextResponse.json({ error: '책 검색 중 오류가 발생했습니다.' }, { status: 500 });
  }
}
```

### STEP 4 — 저장 로직: 무변경 (`recommend/route.ts` 확인만)
- `pubDate` YYYY-MM-DD 저장, book_key 재사용 동작 확인.

### STEP 5 — 표지 폴백: 공용 `<BookCover>` 신설 (결정: '제목 카드')
표지 없는/깨진 책도 식별·안심 선택 가능하게. 백스톱·수동입력 책은 표지가 없을 수 있어 필수.
- 신규: `src/components/BookCover.tsx`
  - props: `src`, `title`, `size`('sm'|'md'|'lg')
  - `<img onError>` → 실패 시 제목 카드로 전환(useState). **빈 문자열 + 로딩실패(404/만료) 둘 다 커버.**
  - 제목 카드: 따뜻한 단색 배경(surface-muted 계열) · 표지와 같은 2:3 박스 · 제목 가운데 line-clamp(lg 3줄/md 2줄) · 🌱 좌상단(sm 생략) · sm은 제목 1줄.
- 배선(이번 전환 범위): `recommend/create/page.tsx`(드롭다운 img·선택카드 img — 현재 폴백 없음), `SearchClient.tsx`(현재 📖 단독 → BookCover로 교체).
- 후속(별도): 홈 `BookCard`·`book/[isbn]`·마이페이지 리스트도 BookCover로 통일(전 화면 일관성). API 전환과 섞지 않기 위해 분리.

---

## 4. 저장 파이프라인 영향 — 없음 (확인)
- book_key = isbn13 ‖ link ‖ title → 카카오도 isbn13 정상 → 동일 동작.
- published_date: 어댑터가 YYYY-MM-DD로 잘라 검증 통과(빈 값이면 null, 기존과 동일).
- 기존 저장 books 행: book_key가 ISBN13이면 카카오 isbn13과 매칭되어 **중복 없이 재사용**(데이터 마이그레이션 불필요 — 컬럼 리네임만).

---

## 5. 검증 체크리스트
1. `KAKAO_CLIENT_ID` 미설정 시 500.
2. 제목 검색: 달님 안녕·배고픈 애벌레 → 표지·저자·출판·날짜 정상. (스파이크 통과)
3. 띄어쓰기 무시 매칭 확인(달님안녕).
4. 작가명: 백희나·안녕달.
5. ISBN/바코드 13자리 → target=isbn 단건 정확.
6. 영어 원서: Brown Bear 등. (스파이크 통과)
7. **재정렬**: "동물"·"사과" 상위에 성인소설/문제집 없는지. (스파이크 통과)
8. **개정판**: 구름빵 상위 = 최신판. (스파이크 통과)
9. 기존 기록 있는 책 재검색 → books 재사용(중복 행 X).
10. 저장 후 published_date YYYY-MM-DD.
11. /search ② '기록 없는 책' 섹션 정상 + '첫 기록 남기기'.

---

## 6. 마무리
- **CLAUDE.md 갱신:** 2장 기술스택(알라딘→카카오), 13.1 전체 재서술(카카오·target=isbn·재정렬·카테고리 한계),
  13.3 주석의 '알라딘 링크' 표현 정리, 변경이력 v1.7 추가.
- **스파이크 파일 삭제:** `spike_book_search.mjs`, `spike_rank_test.mjs` (검증용 임시).
- **후속(Deferred TODO에 기록됨):**
  - B안: 국중도 부가기호=77 대량 수집 → 완전 그림책 카탈로그.
  - 출판사 부스트를 books 자동 도출로 전환(시드 축소).
  - 검색 백스톱: 기록 폼 수동입력 경로.

---

## 부록 — 판단/확인
- [결정] 표지 폴백 = '제목 카드' 방식, 공용 `<BookCover>`로 이번 전환에 포함(STEP 5).
- [확인] 카카오 `status` 실제 값 분포(절판/품절 외) — 실데이터로 정규식 보정.
- [사실] "기존 QueryType=ISBN 바코드 분기 유지" 지시 → 현재 코드에 미존재. 본 전환에서 **신규 추가**.
